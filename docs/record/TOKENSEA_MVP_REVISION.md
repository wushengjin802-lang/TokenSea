# TokenSea MVP 版本修订与交付记录

- 2026-07-30：修复企业服务模型“配置路由”向导仍按供应商渠道数组下标和实际模型数组下标配对的问题。候选链路现根据 `channel_model_deployment.provider_instance_id + provider_model_name` 的真实归属生成，兼容一个渠道关联多个模型及多个渠道关联不同数量模型。渠道下拉框保留企业服务模型已选渠道；没有“已批准且可路由”模型的渠道显示为不可选，并明确提示需先在模型部署恢复生产准入，避免选择渠道后实际模型下拉框为空且无原因。未修改模型部署状态或路由数据。

- 2026-07-30：修复路由策略改名后“企业服务模型 → 当前路由”仍显示旧名称的问题。路由更新成功后，控制面会同步所有绑定该 `route_policy_id` 的企业服务模型冗余展示字段；新增 Flyway V45 一次性校正既有路由名称与企业服务模型当前路由名称不一致的数据。对同服务模型已存在绑定路由、但未被任何企业服务模型绑定的历史“生效”路由，V45 改标为“退役”：保留记录和审计历史，不删除数据，也避免这类实际不会参与 Gateway 调用的孤立策略继续被误解为可用。

- 2026-07-30：优化“企业服务模型 → 配置路由”的复用逻辑。已绑定路由（含已生效路由）现在直接回显并使用原路由 ID 更新，不再因每次进入配置向导而自动新增一条草稿路由策略；对已生效路由，Console 禁止“保存草稿”，只允许“保存并生效”，控制面会在写入前执行完整模型映射、真实探测与生产准入校验。路由策略列表新增“创建时间”“更新时间”列，便于区分历史记录和最近修改。未修改数据库结构或历史记录。

- 2026-07-30：修复企业服务模型配置路由时仍按“渠道数组与实际模型数组必须一一对应”校验，导致一个渠道下有多个模型的服务模型出现“服务模型映射无效”的问题。路由策略的保存/生效校验和 Gateway 运行时解析现均按 `channel_model_deployment` 的真实渠道归属逐模型匹配；每个实际模型必须唯一匹配一个已选渠道，且每个已选渠道至少关联一个模型。未修改历史服务模型、路由、渠道或价格数据，也未新增迁移。

- 2026-07-30：修复企业服务模型编辑时错误要求“供应商渠道数量必须与实际模型数量一致”的问题。服务模型映射改为根据 `channel_model_deployment` 真实归属逐模型解析：一个渠道可关联多个实际模型，多个渠道也可分别关联不同数量的模型；保存和自动默认路由生成均使用解析后的唯一 `provider_instance_id`，不再依赖两个数组的下标一一对应。仍保留安全校验：每个实际模型必须唯一归属于一个已选渠道、每个已选渠道至少关联一个模型、重复渠道/模型及多渠道重名模型会被拒绝。定向测试覆盖新建和编辑场景下 2 个渠道对应 3 个模型并正确生成 3 条路由候选；`PlatformModelRouteWorkflowTests` 4 项、`AdminFlowSimplificationTests` 12 项全部通过。本次未修改数据库、Console 页面或部署配置，未重启服务。

- 2026-07-30：优化租户工作台在当前账号没有有效租户成员关系时的展示。接口返回 403 不再渲染为红色“工作台加载失败”错误面板，改为普通空状态“暂无可访问的数据”，说明当前账号尚未加入有效租户；其他加载异常仍保留错误样式和重试操作。

- 2026-07-30：删除“租户工作台 → 我的业务空间”页面标题上方重复的“租户工作台”眉题，仅保留页面主标题和数据范围说明。

- 2026-07-30：扩大“参考价格状态 → 当前模型参考价格”列表的“供应商”列宽，由 130px 调整为 180px；表格横向滚动基准同步增加，避免 `volcengine_ark` 等供应商类型被压缩截断。

- 2026-07-30：调整“参考价格状态 → 当前模型参考价格”列表滚动结构。卡片标题栏、表格列标题和底部分页保持固定，纵向滚动改由 Ant Design Table 的数据行容器 `.ant-table-body` 独立承担，外层 `model-table-scroll` 不再整体滚动；通过 `ResizeObserver` 按桌面端剩余可用高度动态计算数据区高度，窗口尺寸变化时同步更新，移动端继续使用自然高度。横向滚动保留在表格数据区域。补充 Console 资源契约测试；未修改接口、数据库和价格数据。

- 2026-07-30：修复“参考价格状态 → 当前模型参考价格”搜索框仅匹配模型名和展示名称、不能匹配供应商类型的问题。`keyword` 查询现在同时匹配 `provider_type`、模型名和展示名称，分页越界时的总数兜底查询使用同一条件；因此输入 `volcengine_ark` 可检索对应供应商的参考价格记录。新增控制面回归测试，覆盖供应商类型关键词搜索。

- 2026-07-30：实施公共参考价“模型原始厂商识别 + 跨来源完整模型名精确绑定 + 分级回退”，不新增官方价格采集或人工官方价录入功能。新增 Flyway V44：`reference_model_origin_rule` 内置豆包、DeepSeek、千问、Kimi、智谱、MiMo 等模型前缀与原始厂商规则；`reference_price_binding` 保留目标调用渠道、真实价格来源渠道、原始厂商、匹配类型、置信度和匹配依据；`v_effective_deployment_reference_price` 按官方精确、厂商精确、聚合渠道精确、内置快照精确的优先级选择当前参考价。只允许完整模型名和区域精确/全局匹配，不删除日期版本、不做相似字符串匹配，也不把 AIHubMix、OpenRouter 等聚合价格改写成厂商官方价格。公共价格同步完成和供应商模型发现完成后立即刷新绑定，后台每 60 秒继续兜底刷新。模型部署页面新增官方/厂商/聚合/内置/暂无参考价状态，并展示真实来源渠道、模型原始厂商、匹配类型、置信度、依据、原币种和更新时间；Gateway 的公共参考价回退改为使用同一有效绑定视图，USD 等外币仍按月度汇率折算预算。集成测试验证 `volcengine_ark / doubao-seed-2-0-mini-260428` 自动使用 `models.dev / aihubmix / global / USD` 同名记录作为聚合参考价，同厂商精确价出现后自动优先，`260429` 不会误用 `260428` 价格。V1→V44 迁移、控制面绑定测试、价格同步 5 项、模型生命周期 3 项、Gateway Runtime 40 项及 14 个子测试、Console 资源契约和生产构建均通过。本次未迁移当前业务库、未重启本地服务或 Docker。

- 2026-07-30：压缩“参考价格状态”页两个列表区块的标题高度与区块间距，并将“自动价格来源”和“当前模型参考价格”的标题栏固定在各自卡片顶部；两个表格改为独立数据滚动区，表格列标题使用 sticky 固定，只滚动数据行。页面纵向间距由 14px 调整为 10px，区块标题栏由 64px 压缩至 44px，标题字号调整为 16px，模型区标题与下方表格间空白同步收紧，以显示更多参考价格记录。补充 Console 资源契约；未修改后端接口、数据库和价格选择逻辑。

- 2026-07-30：精简“模型配置 → 参考价格状态”页面说明文案，删除“系统来源只读；异常时可手工重试，正常情况下无需操作。”和“自动选择未过期且优先级最高的来源；缺失价格不影响模型调用。”两段辅助说明；保留来源状态、手工重试、参考价查询及自动选择逻辑不变。

- 2026-07-30：优化“企业服务模型—路由策略”循环配置流程。新建企业服务模型草稿时不再要求选择尚未存在的路由策略；控制面会根据已选供应商渠道和已审核部署模型自动创建、绑定默认路由草稿，并按映射顺序生成候选优先级，多候选默认开启故障切换。新建保存成功后，Console 自动在当前页面打开“配置路由”向导，锁定当前企业服务模型，允许调整路由名称、优先级/加权策略、故障切换和候选顺序，并提供“保存草稿”“保存并生效”；不再要求往返独立路由页面和企业服务模型页面。对于已发布模型，再次配置路由时保留原生效路由和当前发布状态，创建未绑定的新路由草稿；仅保存草稿不会影响现网调用，执行“保存并生效”成功后才绑定新路由并将企业服务模型转回草稿，等待发布检查和重新发布。企业服务模型编辑请求未携带路由字段时保留现有绑定，避免通用表单误清空路由。独立“路由策略”页面继续保留，用于全局查看、后续版本治理和退役。新增 3 项控制面测试，覆盖自动创建绑定默认路由、已发布模型创建新路由草稿、编辑时保留手工路由；相关 Java 测试、Console 资源契约和生产构建通过。本次未新增数据库迁移，未重启服务或重建 Docker。

- 2026-07-30：优化“用量分析—看板统计”排行型卡片的纵向展示。模型使用量、租户成本、项目用量、供应商图例和 Virtual Key TOP10 均改为卡片内部垂直滚动，保留卡片标题固定；Virtual Key 表头在滚动时保持吸附。供应商成本占比继续保留左侧环形图不滚动，右侧图例独立滚动，避免供应商较多时图例被裁切。折线趋势、应用趋势和环形图本体不增加垂直滚动。统一使用细滚动条和稳定滚动槽，避免内容出现时挤压布局。本次仅修改 Console 样式、资源契约测试和修订记录，未修改接口、统计口径、数据库或部署配置。

- 2026-07-30：修复“快速开始”发送真实测试请求期间网关响应区状态文案不一致的问题。请求进行中时，右侧“网关响应”现在显示“请求中”状态和“正在发送真实请求，请等待网关响应”；请求失败后显示“调用失败”及重试提示，不再继续显示“尚未发送真实请求”。补充 Console 资源契约断言。本次仅修改前端页面、测试和修订记录，未修改 Gateway、控制面或数据库。

- 2026-07-30：调整 TokenSea 首页右上角登录态操作。未登录状态仅显示“登录”，不再展示“进入控制台”；登录后显示“控制台”和用户头像，并将按钮文案由“进入控制台”精简为“控制台”。同步更新 Console 资源契约，确保控制台按钮必须绑定 `loggedIn` 条件且旧文案不再出现。本次仅修改前端代码和修订记录，未重启服务或重建 Docker。

- 2026-07-30：优化 TokenSea 首页页内导航体验。“产品能力”“解决方案”“开发者文档”统一改为首页锚点导航，点击后平滑滚动到对应内容区；顶部导航栏改为吸顶显示，滚动到任一内容区后仍保持品牌、三个导航入口、进入控制台和登录/用户入口可见，便于继续切换。为桌面端和移动端分别设置锚点顶部偏移，避免目标标题被吸顶导航遮挡；补充 Console 资源契约测试。本次仅修改前端代码和修订记录，未重启服务或重建 Docker。

- 2026-07-30：精简“模型配置”菜单，隐藏“模型候选”和“模型别名审核”两个偏底层治理页面。判断依据：模型候选是官方价格同步和渠道模型发现产生的中间记录，渠道真实发现后会进入模型部署治理；供应商模型别名主要用于价格匹配、版本关系和异常治理，不是平台管理员的日常主流程。两项后端能力、API、数据和直达路由继续保留，便于异常排查和后续高级治理，不删除历史记录。模型配置主流程调整为“供应商渠道 → 模型部署 → 企业服务模型 → 路由策略 → 参考价格状态”，并更新模型部署页说明。本次仅修改 Console 菜单、页面说明和修订记录，未修改后端、数据库或业务规则。

- 2026-07-30：统一 TokenSea 首页登录前后的右上角操作顺序。将“进入控制台”固定放在第一位；未登录时其后显示“登录”，登录后同一位置显示用户头像，避免登录状态切换后两个操作的位置互换。补充 Console 资源契约断言，防止后续改动再次改变按钮顺序。本次仅修改前端代码和修订记录，未重启服务或重建 Docker。

- 2026-07-30：优化“参考价格状态”页面自动价格来源和当前模型参考价格接口性能。根因是 `/overview`、`/sources`、`/models` 三个读取接口每次请求前都会执行全表过期状态更新；当前约 8107 条价格记录下，即使状态没有变化仍会改写全部记录，并在并发加载时产生重复写入与锁等待。现移除读取接口中的状态刷新，改为继续由小时级调度任务维护持久状态；刷新 SQL 仅更新实际发生 CURRENT/STALE 变化的记录，同时列表和概览直接根据 `stale_at` 实时计算过期状态。模型列表将“总数统计 + 分页查询”由两次完整视图扫描合并为单次窗口计数，超出页码时才执行兜底统计；页面初始化改为概览、来源、模型列表三路并行加载。基于当前本地 PostgreSQL 的 `EXPLAIN ANALYZE`：原无变化状态刷新约 634.9ms，修改后约 14.6ms；原模型 count+page 两次视图扫描约 97ms，合并后约 53.2ms；来源列表自身约 11.2ms。新增 4 项回归测试，覆盖读取接口不触发写操作、模型分页单查询、状态刷新只写变化行和来源列表实时过期判断；相关后端测试与 Console 生产构建通过。本次未重启服务、未重建 Docker、未执行数据库迁移。

- 2026-07-29：修正“模型部署”仍依赖旧正式价格流程的问题，使页面和生产准入与自动参考价格方案一致。模型部署列表不再显示“缺少正式价格”，改为按供应商类型、实际模型名和区域精确匹配 `v_current_public_model_price_reference`，展示“已有参考价/暂无参考价”、参考来源、币种、输入/输出单价和更新时间；新增 `GET /api/model-deployment-governance/{id}/reference-price`，未匹配时明确提示“公共参考库暂无精确匹配价格，该状态不影响模型生产准入和调用”。生产准入移除正式 `price_version` 前置条件，仅保留供应商渠道启用、最近一次真实能力探测通过和平台管理员确认；价格版本或价格状态变化不再反向修改模型生产状态。新增 Flyway V42，将已有“健康、候选、最近真实探测通过”的模型迁移为待审核，不处理未探测或探测失败模型。更新 Doubao 接入文档，删除已下线的“价格源管理—测试解析—价格差异审核”步骤，改为“参考价格状态”自动查询。新增 PostgreSQL 集成测试，覆盖 `volcengine_ark / doubao-seed-2-0-lite-260215` 精确参考价匹配、其他 Seed 2.0 型号暂无参考价的非阻断语义，以及正式价格状态变化不覆盖已拒绝的生产状态；同步更新 API 文档和 Console 资源契约。未迁移当前业务数据库，未重启本地服务。

- 2026-07-29：新增《TokenSea 配置一个新模型操作步骤》V1.0，保存为 `docs/TokenSea_配置一个新模型操作步骤_V1.0_20260729.md`。文档按当前实际菜单和运行逻辑，梳理供应商渠道、`INFERENCE` 密钥托管、连接测试、模型发现与候选审核、模型部署、能力探测、自动参考价格、企业服务模型、路由策略、租户/项目/应用授权、Virtual Key 和最终调用验证的完整操作流程；明确公共价格由 LiteLLM、models.dev 和离线快照自动同步，每日更新，仅用于成本参考，不要求为新模型人工配置价格源，也不影响模型调用。补充最终验收清单和常见故障排查。本次仅新增 Markdown 文档和修订记录，未修改业务代码、数据库或运行环境。

- 2026-07-29：完成当前 TokenSea 业务数据库与本地混合开发环境升级。迁移前在 PostgreSQL 数据卷内生成并校验完整压缩备份 `/var/lib/postgresql/data/tokensea_backups/tokensea_pre_v41_20260729_090155.dump`；业务库由 Flyway V9 成功升级至 V41。按研发模式保留 PostgreSQL、Redis、Egress Proxy、Headless Fetcher、LiteLLM Runtime Core 在 Docker 中运行，Console、Control Plane、Gateway Runtime 重新以 Windows 本地进程启动并通过健康检查。真实公网价格同步结果：LiteLLM Cost Map HTTP 200，解析并写入 2695 条参考价格；models.dev HTTP 200，解析并写入 5406 条参考价格；当前参考价视图合并离线快照与在线来源。同步过程中修复 Egress Proxy 对“一个安全公网地址 + 一个非公网/保留地址”的混合 DNS 响应整体拒绝问题：现仅丢弃不安全候选并固定连接安全公网地址，全部候选不安全时仍拒绝；回归测试 27 项通过。由于 Docker Hub 拉取 `python:3.12-alpine` 元数据出现 EOF，研发 Compose 新增本地 `services/egress-proxy/app` 只读挂载，仅混合开发模式生效，生产 Compose 不受影响。未删除数据库或 Redis 数据卷。

- 2026-07-29：完成“公共参考价格自动初始化与每日更新”代码实施及中断后复核。新增 Flyway V41，使用 `managed_by/source_purpose/publish_target` 隔离系统参考源和正式价格源；LiteLLM Cost Map、models.dev 自动启用、启动后立即入队并按 `P1D` 每日更新，入队时同步前移 `next_run_at` 并增加最多 15 分钟随机抖动，避免启动初始化与调度轮询重复执行。发布包内置 `reference-price-bootstrap.json` 离线快照，启动时幂等导入；公共参考价直接更新 `public_model_price_reference`，不生成 `provider_price_diff`，不进入逐模型审核或正式结算。当前价视图按过期状态、来源优先级、置信度和观测时间自动选取，在线来源过期后回退到离线快照；在线失败保留最近成功价格，并按 30 分钟、2 小时、次日退避。新增 `ReferenceModelMatcher`，保留 OpenRouter 等聚合供应商语义，避免与直连模型错误归并。显式关闭 `TOKENSEA_REFERENCE_PRICE_ENABLED` 时，系统参考源自动暂停。Console 新增“参考价格状态”页面并隐藏日常人工价格源配置入口；系统来源在旧 CRUD 中只读。验证结果：Control Plane 普通全量回归 170 项，0 失败、0 错误、21 项条件跳过；PostgreSQL 16 上 V1→V41、历史 V6→V41、价格源字段持久化、JSONB 审核、离线快照幂等及在线优先级/过期回退 5 条实库路径逐项通过；在线公共参考写入、调度前移和并发入队幂等集成测试通过；Console 资源契约、TypeScript 检查和生产构建通过；Egress Proxy 26 项测试通过；Compose 配置解析通过。真实 LiteLLM/models.dev 公网同步未执行；本次未迁移现有业务数据库、未重建或重启 Docker。

- 2026-07-29：新增《TokenSea 公共参考价格自动初始化与每日更新实施方案》V1.0，保存为 `docs/TokenSea_公共参考价格自动初始化与每日更新实施方案_V1.0_20260729.md`。根据“价格仅用于模型选型、趋势和成本估算参考，不参与供应商结算或对客收费”的最新产品口径，将现有人工新建价格源、测试获取、测试解析、启用和逐模型审核流程调整为系统内置参考价格快照、部署后自动初始化、LiteLLM/models.dev 等结构化来源每日自动更新、多来源自动合并、最近成功数据兜底和异常时仅运维介入。方案详细列出产品页面简化、后端模块、V41 数据迁移、自动调度、新模型自动导入、参考成本与实际成本隔离、离线部署、测试验收、排期和文件级改造清单。本次仅新增实施方案和修订记录，未修改业务代码、数据库或运行环境。

- 2026-07-29：修复新建价格源处于“草稿”状态时点击“测试获取”被 Egress Proxy 拒绝并提示 `Tunnel failed, got: 403` 的问题。根因是动态出口策略仅汇总 `ACTIVE/PAUSED/DEGRADED` 价格源，未包含需要先测试后启用的 `DRAFT` 价格源。现将草稿价格源的精确 `official_hosts` 纳入动态策略，同时为 Egress Proxy 增加“未命中时按需拉取一次最新动态策略再判定”的机制，消除后台 15 秒轮询带来的新建后短暂 403。仍保持精确域名、允许端口和公网 IP 校验，不支持通配符，不包含已停用来源。Control Plane 对残余隧道 403 改为中文安全提示。补充控制面草稿域名策略测试和 Egress Proxy 按需刷新/仍拒绝未授权域名测试；未修改数据库结构，未启动或重建 Docker。

- 2026-07-29：修复价格源新建表单的两项联动问题。将基础字段顺序调整为“供应商类型 → 价格源名称 → 适配器 → 官方来源地址 → 区域 → 币种 → 同步周期”；首次选择供应商时自动生成推荐名称，用户手工修改名称后再次切换其他配置不再覆盖，编辑已有价格源时始终保留原名称。通用文档预设在使用 `DETERMINISTIC_LLM` 时由后端明确返回 `config.llmEnabled=true`，前端切换提取模式及保存前也会同步修正该标志，避免出现“界面选择确定性提取 + LLM 补充，但底层配置仍关闭 LLM”而被后端拒绝的问题。补充豆包和未知供应商的预设回归断言及 Console 资源契约；未修改数据库结构，未启动或重建 Docker。

- 2026-07-29：精简“高级治理 → 价格源管理 → 新建价格源”弹框。通用 `DataPage` 新增“基础配置 + 高级配置折叠”能力，价格源首屏仅保留价格源名称、供应商类型、采集适配器、官方来源地址、价格区域、默认币种和同步周期，其余连接器、可信等级、发布策略、提取参数、认证、官方域名和适配器 JSON 等字段收纳到“采集与治理高级配置”。新增后端 `GET /api/provider-price-connectors/provider-options`，按供应商模板返回价格源推荐配置；当前内置 DeepSeek、千问、Kimi、Xiaomi MiMo、智谱和火山方舟/豆包的官方来源地址、精确官方域名、区域、币种、适配器和提取方式，未知供应商安全回退为通用文档、人工审核且不编造官方 URL。选择供应商后前端自动带出推荐值；手工填写官方来源地址且官方域名为空或仍为系统推荐值时，会从 HTTPS URL 自动识别精确主机名。补充后端预设单元测试、Console 资源契约和 API 文档；未修改数据库结构，未启动或重建 Docker。

- 2026-07-29：修正价格源连接器命名与 Doubao 新模型接入文档。将后端 `HTTP_DOCUMENT` 连接器的前端显示名称由“官方网页或结构化文档”统一为“通用价格文档（HTML/CSV/JSON/PDF）”，与适配器和操作文档保持一致，并增加连接器注册测试。补充 Doubao-Seed-2.0-Lite 价格源推荐配置：适配器、无需认证、官方来源地址 `https://www.volcengine.com/product/doubao/`、官方域名 `www.volcengine.com`、获取模式及域名填写规则；明确模型调用域名 `ark.cn-beijing.volces.com` 不应填入价格文档来源域名。本次未修改数据库结构、真实价格数据或运行环境。

- 2026-07-29：新增《TokenSea 新模型接入操作说明——Doubao-Seed-2.0-Lite》V1.0，保存为 `docs/TokenSea_新模型接入操作说明_Doubao-Seed-2.0-Lite_V1.0_20260729.md`。文档按当前 Console 菜单和治理链路，简要说明火山方舟供应商渠道、`INFERENCE` 密钥托管、连接测试、模型发现与能力探测、通用官方价格源、价格文档提取审核、价格差异批准、模型生效价格、企业服务模型、路由策略、租户授权和 Virtual Key 调用全过程；示例采用在线推理模型 ID `doubao-seed-2-0-lite-260215`，记录 2026-07-29 官方公开的 0～32K、32K～128K、128K～256K 三档输入/输出价格，并明确缓存起价不能在缺少完整分档证据时自行推断。本次仅新增操作文档和修订记录，未修改业务代码、数据库、配置或运行环境。

- 2026-07-28：完成《TokenSea 多源价格数据接入与通用文档提取实施方案》V1.0，文档保存为 `docs/TokenSea_多源价格数据接入与通用文档提取实施方案_V1.0_20260728.md`。方案基于当前价格源、同步任务、原始快照、专用 Adapter、解析诊断、价格差异审核、正式目录、有效成本价格和用量成本快照代码，规划两阶段升级：第一阶段接入 Azure/AWS/Google 官方目录 API、LiteLLM/models.dev 公共参考源、OpenAI Costs 与供应商账单对账；第二阶段建设 HTML/JSON/CSV/PDF 通用提取及受控 LLM Schema 映射。方案明确后端连接器/提取器/映射/对账模块、数据库迁移、API、Console 页面、业务流程、安全边界、测试、排期、文件级改动和验收标准；现有 Qwen、Kimi、Xiaomi MIMO、智谱解析器继续保留。本次仅新增实施方案和修订记录，未修改业务代码、数据库或运行环境。

- 2026-07-28：修复智谱官方价格源使用 Headless Fetcher 后仍只能获取 4.2 KB 页面壳、页面表格为 0 的问题。根因是智谱定价页主页面位于 `bigmodel.cn`，但核心 JavaScript 资源由 `static.bigmodel.cn` 提供；Headless Fetcher 自身允许子域名请求，而 Egress Proxy 使用精确主机白名单，导致静态资源 CONNECT 请求被 `host_not_allowlisted` 拒绝。新增向前迁移 V35，将 `static.bigmodel.cn` 加入内置智谱价格源 `official_hosts`，并在价格源配置中记录渲染资源域名；同步更新 Flyway 升级断言。该改动不放宽到通配符，不允许阿里 CDN、百度统计或微信等非必要第三方域名。未重建 Docker 容器；当前环境可通过更新价格源配置后由 Egress Proxy 动态策略自动刷新生效。

- 2026-07-27：完成智谱 BigModel 官方价格解析器适配。新增 `ZHIPU_OFFICIAL_PAGE`，针对官方价格页 JavaScript 动态渲染及 Element UI 将表头、表体拆分为两张 DOM 表格的结构，先重建语义价格表，再处理模型单元格 `rowspan` 续行和输入/输出长度阶梯。首期仅解析明确同时给出模型 ID、输入单价和输出单价的文本及视觉 Token 价格；旧版单列 Pricing、搜索、知识库、微调和私有实例价格仅保留诊断证据，不推断输入/输出价格。支持缓存命中价；缓存存储按“每百万 Token/小时”换算为 `CACHE_STORAGE_TOKEN_SECOND`，官方“限时免费”记录为促销性质的 `EXPLICIT_ZERO`；官方未列缓存写入 Token 单价，明确标记为 `NOT_APPLICABLE`，不生成零价。新增 Flyway V34，注册默认暂停、使用独立 Headless Fetcher 的智谱中国区官方价格源，并补齐 GLM-5.1、GLM-4.5 Air、GLM-4.7 FlashX、GLM-5V Turbo、GLM-4.6V FlashX/Flash、GLM-4.5V 模型模板；Console 增加智谱官方价格页适配器。验证结果：智谱解析器 6 项单元测试全部通过，千问/Kimi/MiMo/智谱及公共分析器共 21 项，20 项通过、MiMo 实时测试 1 项按环境条件跳过；价格组件规范化验证通过；Console 资源契约和生产构建通过；Flyway 升级测试 2 项因未配置独立 PostgreSQL 测试凭据按条件跳过。已使用与生产 Headless Fetcher 相同的 User-Agent、官方域名路由守卫、networkidle 等待和 1.5 秒附加等待验证当前官方页面，成功获得包含 GLM-5.2、输入/输出单价和 42 张拆分 DOM 表格的渲染结果；由于本次未执行 V34 业务数据库迁移，现有出口代理尚未从动态策略加载 `bigmodel.cn`，因此未宣称完成生产端到端同步。未修改真实业务价格、未启用价格源、未重启或重建 Docker 服务。

- 2026-07-27：优化“供应商渠道—新建供应商渠道”模板联动。选择供应商模板后，表单自动带出模板协议和默认 API 地址；协议字段由模板决定并设为只读，API 地址保留可编辑以支持代理、私有化或区域化入口；编辑已有渠道时供应商模板不可变，避免模板与渠道协议错配。Control Plane 创建接口增加兜底：未显式传入协议或 API 地址时从供应商模板读取，模板缺少必要默认值时返回明确错误。补充控制器回归测试和 Console 资源契约测试；未修改数据库结构或供应商密钥逻辑。

- 2026-07-27：统一预置常见模型供应商 API 出口白名单，避免每接入一个内置供应商都人工修改 `.env`。`deploy/compose/.env.example` 现默认包含 TokenSea 内置供应商目录使用的 22 个固定 API 主机名，覆盖 OpenAI、Anthropic、Gemini、DeepSeek、Qwen、智谱、豆包、百度千帆、讯飞星火、腾讯混元、Kimi、MiniMax、SiliconFlow、Mistral、Cohere、Groq、Together、Perplexity、xAI 和 Xiaomi MiMo；MiniMax 同时保留既有 `api.minimax.chat` 并加入当前 `api.minimaxi.com`，MiMo 同时包含按量付费和 Token Plan 主机名。已将相同基线同步到本地实际 `deploy/compose/.env`，并保留既有本地开发主机。Azure OpenAI、Bedrock、Vertex AI、企业私有模型及 Custom Provider 等资源专属或自定义域名仍要求安全审核后精确追加，继续禁止通配符。同步更新实施方案、运行验证、风险和上线顺序。本次仅修改环境配置和文档，未重启或重建 Docker 服务。

- 2026-07-27：补充官方价格同步及新模型自动发现实施方案的出口白名单部署步骤。新增 `deploy/compose/.env` 中 `TOKENSEA_ALLOWED_EGRESS_HOSTS` 的配置规则，明确保留已有域名并追加供应商 API 精确主机名，不得填写协议、路径、端口或通配符；以 Xiaomi MiMo 为例区分按量付费 `api.xiaomimimo.com`、Token Plan `token-plan-cn.xiaomimimo.com` 与价格页面 `mimo.mi.com` 的不同治理范围。同步补充 Compose 配置解析、仅重新创建 Control Plane 与 Egress Proxy、连接测试/模型发现复验及 `PROVIDER_TARGET_NOT_ALLOWED`、`PROVIDER_EGRESS_DENIED` 的排查口径，并纳入运行验证、风险清单和推荐实施顺序。本次仅修改实施方案与修订记录，未修改真实 `.env`，未重启或重建 Docker 服务。

- 2026-07-27：完成 Xiaomi MiMo 官方价格解析器适配。沿用千问/Kimi 的可插拔 `PriceSourceAdapter` 架构，新增 `XIAOMI_MIMO_OFFICIAL_PAGE`，按价格源区域和币种分别识别国内 CNY、海外 USD 的 `mimo-v2.5-pro` 与 `mimo-v2.5` 文本 Token 价格；支持缓存命中价，并将官方“缓存写入限时免费”保存为带促销证据的 `EXPLICIT_ZERO` 价格组件。ASR、TTS、联网搜索等非 Token 计费首期仅保留原始证据，不生成零价或错误 Token 价格；页面结构异常时保留旧价并输出解析诊断/Headless 建议。新增 Flyway V31，注册 Xiaomi MiMo 供应商模板、两项文本模型模板及默认暂停的中国区官方价格源，Console 同步增加适配器选项。验证包括 14 项千问/Kimi/MiMo/公共分析器单元测试通过，MiMo 官方实时页面 6 项解析测试通过，Console 资源契约和生产构建通过；Flyway/价格同步定向集成测试共 7 项，3 项通过、4 项因未配置 PostgreSQL 测试凭据按条件跳过。Control Plane 全量 114 项中，本次相关测试均通过，另有 1 项既有 Mockito SQL 断言不一致、2 项因本机 PostgreSQL `tokensea` 密码认证失败而失败，18 项按条件跳过。未执行实际业务数据库迁移、Docker 重建、服务重启或生产价格发布。

- 2026-07-27：修复开发者门户“服务模型列表”页面顶部说明文案与 Gateway 状态卡、模型查询卡之间间距过大的问题。根因是页面同时使用 `list-page-internal-scroll` 的纵向 Flex 布局和页面级 Grid 布局，固定视口高度下 Grid 自动拉伸行高。现统一为纵向 Flex 布局，保持 10px 模块间距，Gateway 状态卡与模型查询区域紧接页面说明展示；仅修改 Console 页面布局，未修改接口和业务逻辑。

- 2026-07-27：合并开发者门户“快速开始”与“Playground”重复能力。保留“快速开始”作为唯一入口，将 Playground 的 Temperature 与最大输出 Token 调试参数并入真实对话请求区；左侧菜单移除 Playground，旧 `/playground` 路由保留兼容跳转至 `/quick-start`。同步压缩“服务模型列表”标题说明与下方 Gateway 概览、模型查询卡片之间的纵向间距，消除全局 `page-header` 外边距与页面网格间距叠加造成的大块空白。补充 Console 资源契约检查。

- 2026-07-27：完成开发者门户三个页面接口复核与浏览器连通性修复。移除“快速开始、服务模型列表、Playground”页头中的“开发者中心 / 开发调试”眉标题；新增统一 Gateway 前端客户端，统一 `/health`、`/health/readiness`、`/v1/models`、`/v1/chat/completions` 的超时、Bearer Virtual Key、请求 ID 和错误解释。修复 Playground 之前从控制面加载模型导致“页面可选但 Key 无权限”的问题，现模型下拉严格来自当前 Virtual Key 的 `/v1/models`；服务模型列表增加网关状态、接口耗时、中文对象类型和权限说明。Gateway Runtime 新增受控 CORS 白名单及局域网 Console 来源规则，修复 Console 39210 跨域访问 Gateway 39212 时浏览器只显示 `Network Error`；前端在通过局域网地址访问 Console 时会把配置中的本机 `localhost` 自动替换为当前访问主机。已通过 Console 类型检查与生产构建、Gateway Python 语法检查及 37 项测试、Gateway CORS 中间件结构检查、Compose 配置解析。本次未重启本地 Gateway 或重新部署 Console。

- 2026-07-27：修复“开发者门户 / 快速开始”检查 Gateway 时浏览器显示 `Network Error`。根因是当前 39212 运行进程未加载最新 CORS 中间件配置，且本地/Compose 环境变量使用 `TOKENSEA_CORS_ALLOWED_ORIGINS`，Gateway 源码仅读取 `TOKENSEA_CORS_ORIGINS`。现兼容两个变量名，本地启动脚本显式设置允许来源，默认仅允许 `http://localhost:39210` 与 `http://127.0.0.1:39210`；补充健康检查 GET 与 OPTIONS 预检回归测试。

- 2026-07-27：首页未登录态改为纯展示模式，不再调用需要登录鉴权的控制台统计和供应商渠道接口。顶部三张实时数量卡改为“统一 API、企业级 Key、智能路由”三类稳定产品能力；右侧控制台区域在未登录或实时数据读取失败时展示明确标注的产品演示，包括 Virtual Key 鉴权、预算限流、模型路由、Fallback 和用量审计链路。已登录用户仍可查看真实网关运营摘要，登录成功后自动加载控制台数据；接口失败时不再显示错误空白或误导性的 0，而是降级到演示视图。本次仅修改 Console 前端页面、样式和修订记录，未增加匿名接口白名单，未修改后端鉴权、数据库或部署配置。

- 2026-07-27：统一优化开发者门户三个页面。快速开始、可访问服务模型、Playground 顶部移除“开发者中心/开发调试”眉标题，仅保留页面主标题与说明。新增前端 Gateway API 客户端，统一 `/health`、`/health/readiness`、`/v1/models`、`/v1/chat/completions` 的地址、超时、Bearer 鉴权、请求 ID 和错误解析。可访问服务模型页面增加模型去重、中文对象类型/归属平台、请求耗时、请求 ID、Key 变更清空旧结果和筛选分页修正；Playground 改为先使用真实 Virtual Key 查询 `/v1/models`，只允许选择该 Key 实际可访问的企业服务模型，再调用 `/v1/chat/completions`，并展示请求 ID、Token、耗时、回复正文和原始响应。Gateway 健康与就绪接口实测 200，未带 Key 查询模型实测 401 `TOKENSEA_AUTH_REQUIRED`；Console 类型检查和生产构建通过。本次未修改后端接口、数据库或业务数据，未重启服务。

- 2026-07-27：首页公开导航与产品内容优化。顶部导航调整为“产品能力、解决方案、开发者文档”，移除面向内部验收的“页面清单”和重复的“文档/开发者文档”入口；右侧新增主操作“进入控制台”，未登录时保留登录入口，已登录时展示用户头像。首页新增六类产品能力、四类企业解决方案和开发者文档概览，保留典型接入/部署生态；“页面清单”仅从公开首页移除，内部路由和开发验收能力不受影响。

- 2026-07-27：完成代码审查遗留项 `TS-CR-R07` 的最小改造。租户、Virtual Key、账户、角色、操作审计和价格差异审核统一采用 `{items,total,page,size}` 服务端分页契约，默认每页 20 条、单次最多 500 条，并增加关键字/状态筛选、安全排序字段白名单和稳定 ID 次排序；Console 通用 `DataPage` 直接复用既有分页能力，账户/角色自定义页面改为服务端查询与翻页。账户角色、账户租户、角色权限由逐条补查改为当前页批量加载，角色关联账户数改为主查询聚合，Virtual Key 的租户/项目/应用名称改为去重后的批量查询；同步适配本地 E2E 脚本和 API 参考文档。新增 7 项分页及 N+1 回归测试，连同既有相关用例共 30 项在 JDK 21 下通过，Control Plane 编译和 Console 生产构建通过；全量 108 项中 2 项仍因本机 PostgreSQL `tokensea` 密码认证失败无法加载 Spring Context，0 项断言失败、17 项条件跳过。本次未新增页面、菜单、数据库表或 Flyway 迁移，未修改业务数据，未重启 Docker 服务。

- 2026-07-27：完成“开发者中心 → 快速开始”真实接口对接。页面直接调用 TokenSea Gateway 的 `/health`、`/health/readiness`、`/v1/models` 和 `/v1/chat/completions`，形成“检查服务 → 验证 Virtual Key → 查询权限模型 → 选择企业服务模型 → 发送真实请求”的接入闭环；成功响应展示请求 ID、返回模型、输入/输出/总 Token、端到端耗时、回复正文和原始响应。代码示例扩展为 Python、JavaScript、cURL 和 Python 流式调用，并始终使用环境变量占位符，不把页面中的真实 Virtual Key 写入示例或浏览器存储。Console `vue-tsc` 与 Vite 生产构建通过，本次未重启或部署服务。

- 2026-07-27：价格源管理默认隐藏永久停用（`DISABLED`）记录，减少非主页面和历史隔离来源对日常管理列表的干扰；选择状态筛选“停用”时仍可查询这些记录及其同步历史，未删除任何价格源、同步任务或原始证据。

- 2026-07-27：统一价格源管理的暂停语义。原“停用”按钮实际调用暂停接口并将状态置为 `PAUSED`，现操作文案改为“暂停”，动态显隐逻辑同步更新；`DISABLED` 仍保留为独立的永久停用状态，不与暂停混用。

- 2026-07-27：修复操作审计来源 IP 显示。审计服务新记录会优先读取 `X-Forwarded-For`、`X-Real-IP`、远端地址，并将 IPv6 回环及 IPv4 映射地址规范为 IPv4；审计查询对历史数据兼容显示 `0:0:0:0:0:0:0:1` 为 `127.0.0.1`，空 IP 按操作来源显示“系统任务”或“未记录”。保留原始数据库审计证据不作历史回写。

- 2026-07-27：优化告警页双 Tab 的视觉层级。页面标题统一为“告警”，当前/历史 Tab 移至说明文案下方，采用轻量文字导航和底部活动指示线，移除顶部悬浮式按钮，避免标题与 Tab 分离造成的突兀布局。

- 2026-07-27：告警中心拆分为“当前告警”和“历史告警”两个 Tab。当前告警仅查询 `OPEN`、`ACKNOWLEDGED` 状态，历史告警仅查询 `RESOLVED` 状态；后端新增 `scope=current|history` 范围过滤，避免前端先加载全部历史记录。已解决告警不再显示确认或解决操作，历史记录仅用于追溯。

- 2026-07-27：修复告警页面状态枚举不一致。告警列表及详情将 `OPEN`、`ACKNOWLEDGED`、`RESOLVED` 分别显示为“待处理、已确认、已解决”；状态筛选同步使用对应后端枚举，避免误用通用资源状态（草稿、启用等）而无法筛选告警。

- 2026-07-27：降低候选模型缺价告警噪声。候选阶段的 `MODEL_PRICE_MISSING` 不再逐模型生成告警，而是按供应商渠道汇总为一条 `PROVIDER_PRICE_COVERAGE_GAP` 信息，标题和明细包含候选总数、健康/待探测/不可用数量及示例模型；保留生产准入对正式价格的强制校验。新增定时收敛任务，会自动解决既有候选阶段逐模型缺价告警并刷新渠道汇总告警；非候选阶段仍保留单模型缺价告警。补充 PostgreSQL 集成回归验证聚合告警及逐模型告警不再产生。

- 2026-07-27：修复新建路由策略时企业服务模型 `Qwen3.7` 被误报“路由名称或服务模型别名无效”。根因是路由接口仅接受小写服务模型标识，而企业服务模型本身允许并已存在含大写字母的标识。路由标识校验现支持字母大小写，仍限制为字母、数字、点、下划线和连字符；补充含大写字母服务模型的路由创建回归测试。

- 2026-07-27：修复 Qwen `qwen3.7-plus` 已有完整官方价格但渠道部署仍显示“价格缺失”的自动匹配问题。根因是官方价格目录将 `0_256000` 这类上下文范围保存为目录档位，而自动匹配仅接受 `DEFAULT`，未生成部署官方价格版本。现优先匹配 `DEFAULT` 档位；不存在时可匹配有效的范围档位，并在派生到渠道部署的价格版本中使用 `DEFAULT` 作为运行时档位，保留原始价格组件范围。补充 PostgreSQL 集成回归覆盖范围档位的自动匹配与运行时价格解析。

- 2026-07-24：完成全项目代码审查与最小修复，并新增 `docs/TokenSea_全项目代码审查与最小修复报告_V1.0_20260724.md`。本次未新增业务功能，修复了 Maven 中文编码未固定、JWT 过滤器将数据库故障误报为 401、API Key 创建/批准缺少操作人、ACTIVE 项目/应用可挂在未启用父级、只读详情不存在时返回 200+null、删除审计 before/after 相同、供应商渠道连接测试有效期硬编码 30 分钟、Gateway 部分失败路径遗漏请求时延指标，以及本地缓存/日志/Windows 特殊文件污染工作区等问题。新增 JWT、Key 归因、组织层级、通用控制器和渠道有效期测试；JDK 21 定向 20 项通过，Control Plane 编译通过，Gateway Runtime 37 项及 14 个子测试通过，Egress Proxy 24 项通过，Console 类型检查和生产构建通过，Compose 配置解析通过。Control Plane 全量 95 项中 2 项因本机 PostgreSQL `tokensea` 密码认证失败而无法加载 Spring Context，Headless pytest 因本机缺少 Playwright 未执行完成，Spring Boot repackage 因运行中的 JAR 被占用未完成；均已在报告中保留为环境或待决问题。本次未执行数据库迁移、业务数据修改、Docker 重启或真实供应商调用。

- 2026-07-24：完成租户—项目—应用—API Key 授权链路重构。租户字段“模型范围”改为“可用服务模型”，Key 字段改为“允许调用的服务模型”；草稿租户可暂不配置模型，但启用及已启用状态必须至少保留一个已发布企业服务模型。租户启用接口不再创建、复用或同步默认 Key，Key 只能在 API Key 页面单独创建。新增 Key 归属层级：默认应用级并强制选择项目和应用，项目级与租户级作为高级选项需显式选择；后端同步校验层级与项目/应用归属。Key 模型下拉仅加载当前租户授权池，创建时继续校验 Key 模型范围是租户授权子集。租户缩小授权池时记录受影响 Key，Gateway 每次请求按“租户授权 ∩ Key 授权”执行，撤销立即生效。新增租户已授权服务模型接口、已发布企业服务模型接口、条件显示/必填表单能力及相关控制面和 Runtime 测试；同步更新 Kimi 与供应商到 Virtual Key 操作文档。

- 2026-07-24：修复 Kimi K2.7 Code 不同计费场景审批时的 revision 冲突，并增加误发布撤销能力。根因是官方价格发布按区域/调用模式/服务层级计算修订号，而数据库唯一约束按“供应商 + 模型 + revision”生效，先批准 Batch 后再批准 Standard 会重复生成 revision=1 并返回 HTTP 500。现同一供应商模型的 revision 全局递增，不同计费场景可同时维护独立 ACTIVE 目录。新增 V29 迁移，为价格差异增加 `REVOKED` 状态；新增 `/api/provider-price-diffs/{id}/revoke`，撤销时停用官方目录、退役派生价格版本、重新计算部署价格和生产路由状态并写审计。Console 的价格差异审核新增“撤销发布”动作和撤销原因表单。已通过 PostgreSQL 集成测试、V1→V29、V6→V29、Console 构建。实际环境中已撤销误批的 `kimi-k2.7-code / BATCH / DEFAULT`，并批准 `STANDARD / DEFAULT`；两个 Moonshot 模型均可正常解析有效成本价格。

- 2026-07-24：完成供应商官方价格解析诊断闭环。新增 `OfficialPriceAnalyzer`，统一 Qwen/Kimi 官方 HTML 价格页的 table 扫描、纵向/横向价格表识别、模型/输入/输出/缓存字段定位、跳过原因和结构化诊断输出；Qwen/Kimi Adapter 保留各自模型、区域、价格组件和子页面规则，仅复用公共分析能力。新增 Flyway `V28__price_parser_diagnostics.sql`，为价格同步任务保存解析状态、页面表格数、匹配价格表数、生成价格记录数和诊断快照；同步成功、解析失败及内容未变化均写入明确状态，执行日志增加 `PARSE_DIAGNOSTICS`。价格源“测试解析”改为专用解析报告卡片，展示 HTTP 状态、页面表格、匹配表格、生成记录、跳过原因、结构指纹、解析提示和标准化样例；价格同步任务列表和执行详情同步展示解析诊断字段。新增公共分析器单元测试并扩展 Qwen/Kimi 解析断言。JDK 21 定向测试共执行 15 项，11 项通过、4 项因未配置 PostgreSQL 集成测试凭据按条件跳过；Console 类型检查及生产构建通过。本次未执行 Docker 重建或实际数据库迁移。

- 2026-07-24：优化“操作审计”列表可读性。时间调整为第一列，列表移除操作人内部 ID 和对象内部 ID，仅显示用户姓名/账号、中文操作、中文对象类型、对象名称、来源 IP 和可读客户端名称；历史 `Identity[...]` 操作人通过用户表实时解析为显示名称或账号，系统任务显示“系统任务”。操作代码和对象类型覆盖当前系统已有审计类型的中文映射，对象名称从变更快照中提取渠道、模型、租户、项目、应用、Key 等业务名称；完整内部 ID、变更前后 JSON 和原始 User-Agent 仍保留在详情中用于追溯。本次不修改历史审计数据。

- 2026-07-24：修复 Moonshot/Kimi 模型部署“有效成本价格”区域匹配问题。原接口在未显式传入区域时错误地从 `channel_model_deployment` 读取不存在的 `region` 字段并回退为 `global`，导致渠道区域 `CN` 无法匹配正式价格版本区域 `cn`。现由 `EffectiveCostPriceResolver` 根据部署关联的供应商渠道自动推断区域，按大小写不敏感方式匹配；`exists()` 同步采用渠道区域，避免生产准入出现同类误判。补充 PostgreSQL 集成回归：渠道区域 `CN`、价格区域 `cn`、请求未传区域时可正确命中合同价，并确认正式价格存在。`kimi-k2.7-code` 的标准实时官方价格已抓取完整，但仍遵循首次价格人工审核规则，需在价格差异审核页面批准 `STANDARD + DEFAULT` 记录后自动生成价格版本并推进部署到 `READY_FOR_REVIEW`。

- 2026-07-24：增强 Qwen 官方价格源解析诊断能力，价格解析结果增加 parseStatus（PRICE_PARSED / PRICE_TABLE_NOT_MATCHED / NO_PRICE_TABLE），用于区分页面无价格表、价格表未匹配和正常解析成功，避免仅依赖 warning 判断。

- 2026-07-24：增强 Qwen 官方价格解析诊断能力。价格源测试结果增加 table 级解析证据，包括识别成功/跳过原因、模型列、输入列、输出列位置，便于供应商页面结构变化后的问题定位。非价格 table 继续跳过，不产生错误告警。

- 2026-07-24：优化 Qwen 官方价格解析器。千问官方价格页面包含大量说明性 table，原逻辑将无法识别模型/输入/输出表头的普通 table 记录为解析告警，导致价格源测试出现误报。调整为仅对具备价格表特征的 table 执行解析，非价格 table 静默跳过，降低供应商页面结构变化带来的误报。

- 2026-07-24：完成 Moonshot 渠道异常原因展示与出口白名单运行修复。Console 的供应商渠道详情改为实时读取连接测试详情，连接测试失败后自动刷新列表与详情；后端保存完整错误码和中文说明。进一步修复 MyBatis-Plus 默认忽略 null 更新导致连接测试成功后历史 `lastConnectionTestError` 无法清空的问题，为连接测试结果字段启用显式空值更新。重新创建 Egress Proxy 容器加载 `api.moonshot.cn` 白名单，重启本地 Control Plane 与 Console，并执行真实 Moonshot `/models` 连接测试；最终状态为健康/成功，目标 `api.moonshot.cn:443`，历史异常已清空，Console、Control Plane 与 Egress Proxy 均健康。Docker Hub 拉取基础镜像时曾出现 EOF，因此代理复用了当前已验证镜像，仅重建容器加载环境配置，未操作 PostgreSQL、Redis 或 Runtime Core。

- 2026-07-24：修复 Moonshot 渠道连接测试失败后详情仍显示空异常原因。根因是后端已保存失败状态和错误信息，但 Console 捕获业务失败后不刷新列表，详情弹框继续使用旧列表行。现为供应商渠道增加实时“连接测试详情”查询，连接测试失败后自动刷新当前列表与已打开详情；后端保存异常时同时记录错误码和中文说明，例如 `PROVIDER_TARGET_NOT_ALLOWED: 供应商目标未列入出口白名单`。补充 Console 资源契约测试和 Moonshot 官方 API Base 白名单单元测试。当前 `.env` 已包含 `api.moonshot.cn`，运行中的 Control Plane 需重新加载环境配置后才能消除该白名单错误。

- 2026-07-24：修复“模型候选—验证渠道”状态判断问题。原核验接口将生产可用部署统计强依赖价格匹配，导致渠道实际验证成功但因缺少价格仍显示不可用；现拆分渠道验证与价格状态，渠道匹配成功即可返回 `CHANNEL_VERIFIED`，价格缺失只影响成本配置，不影响技术连通性判断。同时保留生产准入条件，不自动发布模型。本次修改控制面候选核验逻辑，未修改数据库结构。

- 2026-07-24：精简“用量分析—看板统计”顶部 KPI，移除“缓存命中率”和“缓存净节省”两张卡片；后端统计接口、明细列表中的缓存 Token、缓存命中率、缓存净节省及缓存计费数据继续保留，仅调整看板展示。

- 2026-07-24：修复“模型候选—核验渠道”误导性成功提示及可用性不透明问题。原“渠道验证次数”实际表示当前精确匹配的渠道部署数量，并非点击次数；当没有渠道部署时，接口仍返回 200，通用页面固定提示“验证渠道成功”，导致用户误以为模型已验证。现改为“已匹配渠道数”，候选列表新增“当前可用性”和详情中的“生产可用部署数”；仅价格发现明确显示“不可调用”，渠道发现明确显示“待生产准入”。核验接口返回匹配渠道数、可用部署数、供应商渠道状态及针对性处理提示，前端按结果显示警告或成功，不再把接口执行成功等同于渠道验证通过。当前 Moonshot 渠道还存在两个实际配置问题：出口白名单缺少 `api.moonshot.cn`，API Base 缺少 `/v1`；已补充本地出口白名单并将当前渠道地址修正为 `https://api.moonshot.cn/v1`，同时清除旧连接测试结果，渠道仍保持暂停，需重启 Control Plane 后重新连接测试、启用并执行模型发现。本次未删除数据卷，也未自动将候选投入生产路由。Control Plane 全量测试共 85 项，0 失败、0 错误、15 项条件跳过；Console 类型检查及生产构建通过。

- 2026-07-24：完善“供应商渠道”详情弹框的健康异常信息。详情新增最近连接测试状态、最近连接测试时间、异常原因、测试目标主机和目标端口；后端原有 `lastConnectionTestError` 等字段直接用于展示，不修改连接测试、健康状态计算、供应商密钥或数据库结构。当前 Moonshot 渠道保存的异常原因为“供应商目标未列入出口白名单”。

- 2026-07-24：调整“供应商官方价格目录”页面展示。页面说明“默认按每百万 Token 维护缓存未命中输入、缓存命中输入、缓存写入和输出价格；长上下文、TTL 等变体使用高级组件数组维护。”改为单行显示，不再自动换行；列表中的“缓存命中价格模式”列移动到“币种”之后。仅调整 Console 资源配置，不修改价格数据、计费规则或后端接口。

- 2026-07-24：将《TokenSea 接入 Kimi 并生成可用 Virtual Key 操作指导书》从 V1.0 修订为 V1.1，并保留原文件路径以兼容既有引用。文档不再以人工录入 Kimi 官方价格为主流程，改为“内置 Kimi 官方价格源 → 独立 Headless Fetcher → 测试获取/测试解析 → 每日同步 → 价格差异审核 → 官方价格目录 → 模型候选 → 渠道 `/v1/models` 真实发现 → 独立 LIVE_PROBE → 人工生产准入 → 路由与企业服务模型 → Virtual Key”的现行流程；补充 `PRICE_ONLY/CHANNEL_VERIFIED` 候选状态、`DISCOVERED/HEALTHY/MATCHED_OFFICIAL/APPROVED` 部署四维状态、V24 URL fragment 去重、正式成本价格优先级、模型消失与恢复治理、Headless 安全边界、完整用量与成本快照验收清单。同步更新菜单名称、验证脚本调用方式、常见问题和最小可执行路径。本次仅修改 Markdown 文档和修订记录，未修改代码、数据库或部署环境。

- 2026-07-24：增强“模型候选”渠道验证记录能力。字段调整：页面“渠道验证次数”统一为“已匹配渠道数”，避免将匹配数量误认为操作次数；新增最近验证时间、验证结果、验证说明、验证次数。模型候选渠道核验接口现在每次点击都会增加验证次数，记录 PASS/FAILED 结果和失败原因，并写入操作审计。数据库新增 `last_verification_at`、`last_verification_result`、`last_verification_message`、`verification_attempt_count` 字段，用于后续追踪模型发现质量和渠道治理过程。本次修改涉及控制面、Console 展示和 Flyway 迁移，未修改模型路由准入规则。

- 2026-07-24：简化“模型别名审核”的批准操作。点击“批准”后直接调用现有批准接口，不再弹出“批准意见”填写框；“拒绝”仍要求填写拒绝原因。后端原本已支持无审批意见直接批准，本次仅调整 Console 操作配置，不修改审核规则、别名循环校验、自动价格重匹配、审计记录或数据库结构。

- 2026-07-24：修复调用日志与用量分析的最新记录不同步及成本快照为空。用量分析原“结束时间”只在页面首次加载时取当前时间，后续刷新仍使用旧上限，现改为默认随刷新、查询和 Tab 切换自动推进到当前时间；用户手工修改结束时间后则保留其历史区间。Gateway 成本快照 INSERT 缺少 usage_source 占位符，导致 `UNKNOWN` 被错误按 JSON 解析并使 usage_record 已写入但 usage_cost_snapshot 失败，现修正 42 参数映射并补充回归断言。清理一个遗留的孤立旧 Gateway worker，重新处理 8 条 usage outbox，并新增 V25 对历史缺失快照按 usage_record 与请求时 price_version 进行幂等回填；当前数据库 13 条用量记录均已有成本快照，usage outbox 全部 DONE。Gateway 测试 36/36、14 个子测试通过，JDK 21 `FlywayUpgradeIntegrationTests` 通过，Console 生产构建通过。

- 2026-07-24：修复“用量分析—明细列表”选择较多字段后表格被强制压缩、文字逐字换行和页面错乱的问题。明细表现在根据当前可见字段动态计算最小宽度，为不同字段设置合理列宽；字段较多时仅表格区域出现横向滚动，不再压缩整页。首个可见字段与右侧“详情”操作列固定，表头继续保持顶部吸附；单元格最多显示两行，悬停与键盘焦点状态保持一致。字段选择、浏览器本地记忆和当前可见字段导出逻辑保持不变。`npm run console:build` 已通过。

- 2026-07-24：为“用量分析—明细列表”增加表头字段选择器。默认仅展示时间、租户、项目、应用、Virtual Key、供应商、服务模型、总 Token、成本、币种、耗时和状态，Token 拆分、缓存、实际模型及计费状态等字段可由用户按需勾选；支持全部显示、恢复默认和至少保留一个字段，选择结果保存在浏览器本地。当前页 CSV 导出同步采用用户当前可见字段；Token 拆分、缓存和计费细节仍可在调用链的成本快照中完整查看。本次仅修改 Console 展示与导出字段，不修改用量接口、统计口径或历史数据。

- 2026-07-24：修复“用量分析—明细列表—成本快照”无内容的问题。根因有两处：Gateway Runtime 写入 `usage_cost_snapshot` 时 SQL 已扩展为 42 个字段，但 `VALUES` 仅声明到 `$41`；同时旧调用链可能以对象形式传递 `price_components`、`cost_components`，而数据库约束要求保存为 JSON 数组。两类错误都会造成 `usage_record` 已落库、成本快照插入失败，详情接口最终只能返回空快照。现已补齐 `$42` 占位符，并统一通过组件标准化函数将对象结构转换为带 `componentType` 的数组后落库；回归测试校验占位符数量、绑定参数数量、计费状态及两类组件的数组结构。本次未重启 Gateway，也未自动修复历史缺失快照；新代码生效后新请求会正常写入，历史失败事件需结合 Outbox/死信单独恢复。

- 2026-07-24：修复模型部署“有效成本价格”弹窗中缓存价格显示不完整的问题。`EffectiveCostPriceResolver` 响应新增缓存命中单价、缓存命中模式、缓存写入单价和缓存写入模式；通用关联结果弹窗按价格模式显示“0、沿用输入价格、不适用、待确认”，不再将所有空单价统一显示为破折号。新增集成断言验证 `deepseek-v4-flash` 同类价格结构中的缓存命中价可返回、缓存写入 `NOT_APPLICABLE` 可被识别。本次不修改价格数据、价格版本或计费规则。

- 2026-07-24：调整“供应商渠道”列表紧凑方案。恢复表头与数据单元格原有字号，仅保留紧凑内边距、固定列布局和窄屏隐藏策略；操作按钮进一步缩短高度并减小圆角，确保按钮更小但列表文字仍保持原有可读性，水平滚动条继续保持关闭。

- 2026-07-24：压缩“供应商渠道”列表并取消水平滚动。列表隐藏低频的协议、区域和负责人字段，但在新建/编辑表单及详情中完整保留；渠道名称、供应商模板、API 地址、环境、RPM、TPM、密钥状态、健康状态和启停状态采用固定紧凑列宽。操作区按钮缩小并按两行网格排列；窄屏时逐级隐藏 RPM、TPM、密钥状态、环境和供应商模板，确保列表始终在可用宽度内展示，不出现水平滚动条。本次仅修改 Console 展示，不修改供应商渠道接口或业务数据。

- 2026-07-24：修复暂停租户重新启用时重复创建默认 Virtual Key 导致 500 的问题。启用接口现在会优先复用已有默认 Key、同步模型范围并恢复租户状态；只有不存在默认 Key 的租户才创建新 Key。新增控制面测试覆盖暂停租户重新启用，不再触发默认 Key 唯一约束冲突。

- 2026-07-24：修复租户状态弹框选择“启用”时错误调用通用状态接口的问题。前端现会自动调用租户启用接口，以创建或同步默认 Virtual Key；保留暂停等状态变更走原状态接口。同步移除 409 冲突时“重启 Control Plane”的误导性提示，改为说明当前业务状态条件不满足。

- 2026-07-23：完成 V23 在本地业务数据库的实际迁移与价格/模型治理验收。Flyway 从 V22 升级到 V23 且 `success=true`；千问官方价格页测试获取与测试解析均返回 HTTP 200，解析 216 条标准化价格，内置价格源已启用为每日同步，首次价格全部按高风险规则进入 216 条人工审核；Kimi 官方页普通 HTTP 返回 200 但解析记录为 0，适配器明确建议 Headless，因当前代码尚未实现独立 Headless Fetcher，价格源继续保持暂停，未绕过安全门槛启用。使用已托管真实 Key 的 DeepSeek 渠道完成真实生产准入验收：重新获取并审核 `deepseek-v4-flash` 官方 CNY 首价，生成完整 `PROVIDER_OFFICIAL` 价格版本；真实 CHAT 探测访问 `api.deepseek.com` 返回 200、耗时 881ms；部署状态达到 `DISCOVERED + HEALTHY + MATCHED_OFFICIAL + APPROVED + ELIGIBLE + PASSED`；创建并激活验收路由 `573de24b9261837249b49fcedd81fc17`，控制面完整路由校验通过。Console、Control Plane、Gateway 和 Runtime Core 最终健康检查均返回 200。未删除数据库/Redis volume，未输出供应商密钥。

- 2026-07-23：完成千问、Kimi 官方价格自动同步、新模型发现与生产准入治理代码实施。新增 Flyway V23，内置千问中国内地与 Kimi 中国站官方价格源，扩展价格来源获取模式、优先级、价格性质、结构指纹、模型候选、受控别名、合同价层、部署四维生命周期状态及治理事件 Outbox；价格解析改为可插拔适配器，支持千问区域/Batch/思考模式/上下文阶梯/缓存及 Kimi 标准版/HighSpeed/缓存/定价子页面，页面结构变化和新模型首价强制人工审核。模型发现改为启用渠道每 6 小时自动执行，连续 4 次缺失后才真实探测，探测成功仅确认技术健康，进入生产路由前必须具备正式成本价格并由管理员批准；恢复模型必须重新审核。统一成本价解析顺序为 `CONTRACT_PRICE > CHANNEL_ACTUAL > PROVIDER_OFFICIAL`，控制面、路由校验、Runtime 配置和 Gateway Runtime 共用相同语义；新增模型候选、模型别名、生产准入和有效成本价格接口及 Console 页面。验证包括：Control Plane 全量 80 项测试零失败（15 项外部实时或需独立参数的测试按条件跳过），Flyway 全新 V1→V23 与脏 V6→V23 升级均通过；关键 PostgreSQL 集成套件另行实际执行并通过合同价优先级、别名匹配、Outbox 重试、生命周期和价格同步；Gateway Runtime 36 项及 14 个子测试全部通过；Console 资源契约、`vue-tsc` 和 Vite 生产构建通过。未重启或部署现有 Docker 服务，未修改现有业务数据库，所有数据库验证均使用并删除独立临时库。

- 2026-07-23：完成 V21/V22 缓存感知计价迁移与回归验证。Flyway 在隔离 PostgreSQL 数据库中验证 V1→V22 全量升级与脏 V6→V22 升级均成功；新增供应商官方价格目录手工创建、价格版本手工创建的真实 PostgreSQL 集成测试，断言价格组件及缓存摘要实际落库；新增用量分析缓存命中率、缓存净节省 CNY 汇总集成测试，以及 Gateway Runtime 缓存计价告警持久化测试。同步更新既有测试夹具以符合 V21 的真实账户鉴权和缓存组件完整性规则。隔离库完整 Control Plane 套件共执行 71 项，全部通过，另有 2 项依赖外部实时服务的测试按条件跳过。

- 2026-07-23：新增《TokenSea 千问与 Kimi 官方价格自动同步及新模型自动发现实施方案 V1.0》。基于现有 DeepSeek 官方价格同步、渠道模型发现、能力探测、价格目录和路由校验架构，提出可插拔价格适配器、结构化来源/静态 HTML/独立 Headless 获取链、价格同步与模型发现独立调度及统一重匹配、渠道级新模型发现、连续 4 次缺失后真实探测、标准模型/部署版本/受控别名三层治理、生产人工确认、官方价/渠道价/合同价分层及来源冲突处理方案；包含 V23 数据迁移建议、后端 API、Console、文件级改造清单、分阶段计划、测试和验收标准。本次仅新增方案文档，未修改代码、数据库或部署环境。

- 2026-07-23：新增《TokenSea Kimi 采用 models.dev 或 LiteLLM 公共价格参考操作说明》。明确公共价格源只生成公共参考记录，不能直接成为生产生效价格；补充 models.dev、LiteLLM 的启用、测试获取、立即同步、同步任务与原始快照检查、Kimi 精确模型检索、公共价人工核验与转录、模型部署重新匹配、模型生效价格验证及 CNY 汇总规则。文档同时记录当前供应商价格页面仅提供 CNY 下拉选项、USD 原币价格需通过 Control Plane API 保留的现状，并给出 PowerShell 操作示例。

- 2026-07-23：统一全站列表展示规则：顶部工具栏不再展示条数，列表底部始终展示“共 N 条”，有分页时与分页控件同列显示；通用列表及独立的服务模型、调用日志、用量明细、账户/角色、汇率、成本单和租户工作台列表均已接入。全站表格统一禁用水平滚动，保留并强化原有单元格换行，不再通过悬浮框展示截断内容。

- 2026-07-23：优化“操作审计”列表信息展示：移除顶部重复的条数，仅保留列表底部总数；对会被截断的列表单元格启用浏览器悬浮提示，以便查看完整内容。通用数据列表新增可选的顶部条数隐藏和单元格完整值提示配置，仅操作审计启用。

- 2026-07-23：在“操作审计”列表底部增加总条数显示；存在分页时与分页控件同列展示，无分页时单独显示底部总数，顶部条数保留不变。通用数据列表新增可选底部条数配置，仅操作审计启用。

- 2026-07-23：进一步调整“操作审计”列表：操作人 ID、操作、对象 ID 固定为 190px；列表启用无横向滚动布局。客户端和时间继续保持较宽列；窄屏自动释放固定最小宽度并以省略号呈现超长内容，避免出现水平滚动条。

- 2026-07-23：优化“操作审计”列表列宽。检查确认操作人 ID、操作人、操作、对象类型、对象 ID 均未配置固定宽度，保持原有自适应行为；为客户端和时间分别增加 280px、190px 的专用最小宽度，减少长客户端标识与时间值被压缩的情况。通用数据列表新增可选列宽配置，仅操作审计使用。

- 2026-07-23：调整“高级治理”菜单，价格源管理之后依次展示“供应商官方价格”“价格差异审核”；菜单项“供应商官方价格目录”更名为“供应商官方价格”。仅调整导航文案与顺序，不修改页面路由或价格功能。

- 2026-07-23：调整“路由策略”页面：移除无业务必要的“运行治理”眉标，将“路由与 Fallback”统一更名为“路由策略”，包括发布前置校验和接口错误提示；列表前后端统一按 `createdAt`（创建时间）倒序返回与展示。

- 2026-07-23：调整“企业服务模型”列表默认按 `createdAt`（创建时间）倒序展示；前端默认排序与控制面查询排序同步调整，避免刷新后按字符串 ID 排列。

- 2026-07-23：调整“模型生效价格”页面的币种展示，人民币统一以国际货币代码 `CNY` 显示；仅影响该页面的前端展示映射，不修改已生效价格、历史成本或汇率数据。

- 2026-07-23：调整“渠道健康”列表默认按测试时间倒序展示，未测试的渠道保持在列表末尾；仅影响该健康视图的前端排序，不修改渠道数据或连接测试逻辑。

- 2026-07-23：调整“告警事件”列表，发生时间移动到解决时间之前，并设为默认倒序排序；后端告警接口原本已按发生时间倒序返回，前端同步指定默认排序，避免通用列表逻辑重新按 ID 排序。

- 2026-07-23：调整“供应商官方价格目录”默认排序，不再按字符串 ID 排序；前后端统一按生效时间倒序、更新时间倒序展示。通用列表组件支持声明默认主排序及同方向次级排序，其他资源不改变既有排序规则。

- 2026-07-23：调整“供应商官方价格目录”的币种展示，人民币统一以国际货币代码 `CNY` 显示；仅修改该目录的前端选项与列表/详情映射，不修改数据库中的币种值、金额、汇率或其他页面文案。

- 2026-07-23：打通租户—项目—应用—Virtual Key—用量成本的管理闭环。项目和应用列表改为关联汇总视图，直接展示所属名称、关联应用/Key 数、启用 Key、本月请求、Token、CNY 成本、预算使用率/成功率、最近调用及未折算记录；详情抽屉新增本月概览、关联应用、关联 Virtual Key 和最近调用。新增项目/应用关联查询接口与 `ResourceLinkageService`，支持按租户、项目筛选。Virtual Key 创建、审批和生成时强制校验租户、项目、应用真实归属及启用状态，列表返回租户/项目/应用名称；前端选择改为“租户→项目→应用”级联，切换上级会递归清空下级。新增 Flyway V20 数据库触发器和索引，从数据库层阻止应用跨租户挂项目、Key 跨租户/跨项目挂应用。已通过 Control Plane JDK 21 编译、管理员 Key 流程 9 项单元测试、V1→V20 与脏 V6→V20 迁移测试、项目/应用/Key/用量关联 PostgreSQL 集成测试及 Console 生产构建；未重启或部署现有 Docker 服务。

- 2026-07-23：修复 DeepSeek 官方价格源在 Docker Desktop 重启后的同步降级问题。出口代理新增 `host.docker.internal:host-gateway` 显式映射，恢复其到本地控制面的动态白名单访问；控制面同步服务支持 gzip/deflate 响应解压，并在写入 PostgreSQL 文本快照前剔除 NUL 字符，避免上游响应中含 `0x00` 时导致快照写入失败。已通过 5 项同步服务针对性测试（2 项依赖 PostgreSQL 的集成测试按条件跳过），并完成真实同步：DeepSeek 价格源恢复为“启用”，解析 2 条记录，生成 2 条待审核价格差异；Console、Control Plane、Gateway 和 Runtime Core 健康检查均返回 200。待审核差异仍需管理员在价格差异审核页面确认后才会发布，未自动改写既有价格目录。

- 2026-07-23：将内置 DeepSeek 官方价格源切换为中文人民币价格页。新增 Flyway V19，把 `builtin_deepseek_official_price` 的地址更新为 `https://api-docs.deepseek.com/zh-cn/quick_start/pricing/`、默认币种改为 CNY、关闭自动发布并升级解析器版本为 2.0.0；保留 `global` 价格范围，确保现有 USD 官方目录与新 CNY 价格生成 `CURRENCY_CHANGED` 高风险差异，而不是重复生成新增/删除模型。同步改造 `DEEPSEEK_OFFICIAL_PAGE` 解析器，兼容中英文表头、人民币“元/￥/¥”与美元“$/USD”、表格合并单元格和渲染文本兜底；页面识别币种与价格源默认币种不一致时拒绝同步。旧 USD 目录、历史价格版本和历史用量成本快照不修改；旧页面尚未处理的待审核差异在迁移时标记失效，下一次同步生成新的 CNY 差异并要求人工审核。已通过解析器单元测试、V1→V19 全量/脏 V6 升级迁移测试和 Control Plane 编译；未重启或部署现有 Docker 服务。

- 2026-07-22：补齐账户与角色管理能力。确认原系统仅有 `user_account`、`role`、`user_role`、`role_permission`、`user_tenant` 基础表，以及登录、首次管理员初始化接口，没有账户管理、角色管理页面和完整 CRUD 接口。新增 Flyway V18，补充账户最近登录/密码更新时间、角色说明/状态/系统内置标识、关联外键、内置 ADMIN/TENANT_USER 角色和权限目录；新增 `/api/users`、`/api/roles`、`/api/permissions` 管理接口，支持账户创建、编辑、角色与租户授权、启停、密码重置，以及角色创建、编辑、启停、权限配置和安全删除。增加“组织与权限—账户管理/角色管理”页面，并加入当前管理员防自停用、最后一个有效管理员保护、系统内置角色保护和全量审计。登录及 JWT 请求鉴权只加载启用角色。已通过 Control Plane 全量编译、账户控制器单元测试、V1→V18 全量/脏 V6 升级迁移测试和 Console 构建；未重启或部署现有 Docker 服务。

- 2026-07-22：完成 CNY 统一汇总与月度汇率治理。新增 Flyway V17、`fx_rate`/`fx_rate_sync_run`、`tokensea_fx_rate`/`tokensea_fx_amount` 月度换算函数及 `usage_record.budget_currency`；价格目录、价格版本、用量明细、供应商对账与调用链成本快照继续保存和展示原币种原金额，用量看板、内部成本单、成本汇总、预算预占/结算统一按请求发生月份折算为 CNY。新增“成本管理—汇率管理”页面，支持 ECB 官方参考汇率每月 1 日 03:10 自动同步、当前月缺失时每日 03:40 重试、立即同步、自动更新开关、人工覆盖、版本历史和恢复历史自动版本；人工版本优先，自动任务不会覆盖。缺少汇率时禁止按 1:1 计算：网关返回 `TOKENSEA_FX_RATE_MISSING`，统计页面列出未折算记录，成本单标记 `MISSING_FX_RATE` 异常。动态出口策略增加 ECB 官方域名。已通过 Control Plane 编译、Console 构建、Gateway Runtime 31 项规则测试、Compose 配置校验、V1→V17 全量/脏 V6 升级测试、ECB XML 解析与交叉汇率单元测试、人工覆盖/恢复自动版本集成测试及“明细保留 USD、汇总折算 CNY”集成测试；未自动重启或部署现有 Docker 环境。

- 2026-07-22：价格体系一步升级为“页面默认每百万 Token、底层通用计费单位”。新增 V16 数据库迁移，研发环境直接清理历史价格目录、价格版本、成本快照和旧模型价格数据，并将现有非退役路由重置为草稿、企业服务模型解除旧路由和价格引用后重置为草稿；价格字段统一为 `billingBasis + billingQuantity + inputUnitPrice + outputUnitPrice`，Token 默认计费基数为 `1000000`，并支持请求、图片、秒、分钟、字符和音频分钟等计费对象。价格同步、差异审核、官方目录、模型生效价格、路由价格选择、Gateway 成本计算、预算预占、调用成本快照及前端展示均同步改造；Gateway 不再回退旧 `model_price` 价格。更新 Kimi 接入指导书和相关自动化测试。

- 2026-07-22：将平台工作台重构为紧凑型运行驾驶舱。移除占用纵向空间较大的宣传式 Hero 区，标题区、操作按钮和状态标签压缩为同一视觉层级；新增基于真实累计请求、错误、Token 与渠道状态计算的调用成功率、渠道健康率和单请求平均 Token；8 项资源/调用指标改为高密度卡片并增加资源、访问、调用、用量、运行分类及异常色条；运行态、常用操作、待处理风险和渠道健康重新组合为主次两栏，告警与渠道状态支持中文文字、状态点和直接下钻。仅修改 Console 页面与样式，不新增接口、不改变统计口径、不部署 Docker；`npm run console:build` 已通过，Vite 仅保留既有的大包体积提示。

- 2026-07-22：整体美化“用量分析—看板统计”图表区。统一卡片渐变背景、顶部分隔高光、圆角阴影和悬停反馈；供应商成本占比升级为更大的立体环形图、中心总成本/币种/供应商数量，以及带成本、占比、Token、请求数和进度条的图例卡片；模型使用量改为排名徽标、请求摘要、渐变条和 TOP10 占比；租户成本排名由柱形图改为更适合小数值差异的横向排名条；项目用量拆分为 Token 与请求两条独立指标；应用趋势增加面积渐变、数据点、日期刻度和应用汇总标签；Virtual Key 排名表增加表头底色、斑马纹和悬停效果。保持双行固定高度布局，不恢复垂直滚动。

- 2026-07-22：系统性美化“用量分析—看板统计”下方图表。针对图表留白过大、视觉层级弱、排名类图表间距失衡、供应商饼图拥挤、项目双指标重叠、趋势图尺度含义不明确等问题，统一卡片标题/副标题/单位标签和悬停反馈；趋势图明确为按自身峰值归一化并增加百分比刻度及峰值摘要；供应商成本占比改为带分段间隙的增强环形图、成本/Token/请求信息和占比进度条；模型、租户、项目排名改为紧凑横向排行；应用趋势增加面积层、节点、日期轴和应用请求汇总；Virtual Key 排名表强化表头、边框和行悬停。保持两行四列自适应布局，不引入页面纵向滚动。

- 2026-07-22：增强“用量分析—明细列表”调用链下钻交互。除原“调用链”按钮外，点击任意明细行也可打开该请求的调用链弹框；支持键盘聚焦后按 Enter 打开。操作按钮使用事件阻止，避免一次点击触发两次加载，并新增行悬停和键盘焦点样式。

- 2026-07-22：调整调用链弹框中的“成本快照”展示。每个字段统一按“参数：值”在同一行显示，标签和值不再上下换行；超长值保持单行并使用省略号，鼠标悬停可查看完整内容。该样式同时应用于用量分析和调用日志的调用链详情弹框。

- 2026-07-22：压缩“用量分析—明细列表”列宽并取消水平滚动。Virtual Key、供应商、服务模型、实际模型等文本列允许自动换行，Token、成本、币种、耗时和状态等数值列保持紧凑单行；删除“用户标识”展示列。表格改为 100% 固定布局，仅保留纵向内部滚动，调用链详情仍通过内部 request_id 下钻。

- 2026-07-22：调整“用量分析”Tab 与筛选区关系。将“看板统计、明细列表”从独立行移动到筛选栏内，放置在“重置”按钮右侧并作为该行最右侧操作；两个 Tab 共用同一组时间、租户、项目、应用、Virtual Key、供应商和模型筛选。切换 Tab 时会按当前筛选条件重新加载对应数据，避免此前已加载页面继续显示旧筛选结果。

- 2026-07-22：精简“用量分析—明细列表”页面，删除列表上方与主筛选区重复的二次搜索输入框、“查询明细”按钮及右侧“共 N 条”统计条。明细列表统一只使用页面顶部的时间、租户、项目、应用、Virtual Key、供应商和模型筛选；表格排序、分页、调用链查看及导出能力保持不变。

- 2026-07-22：优化“用量分析—看板统计”页面布局。移除“调用状态”筛选；开始时间、结束时间及其余筛选项统一压缩宽度，并固定在同一行展示，查询/重置按钮同步缩小。桌面端页面改为固定可视高度，标题、Tab 和筛选栏保持固定，纵向滚动仅发生在下方 KPI 与图表区域；明细列表继续使用表格内部滚动，避免整个页面滚动。本次仅修改 Console 页面结构与样式，不修改分析接口、统计口径或历史用量数据。

- 2026-07-21：重构“用量分析”为双 Tab 页面，默认首个 Tab 为“看板统计”，第二个 Tab 为“明细列表”。看板基于真实 `usage_record` 数据提供总请求、总 Token、总成本、平均时延、活跃租户、成功率，以及近 30 天趋势、供应商成本占比、模型用量 TOP10、租户成本排名、项目用量分布、应用请求趋势和 Virtual Key TOP10；统一支持时间、租户、项目、应用、Virtual Key、供应商、模型和状态筛选。新增 `/api/usage-analysis/dashboard`、`/details`、`options` 三个只读分析接口，并关联租户、项目、应用、Key、供应商和身份归因数据。明细列表将时间调整为第一列，移除请求 ID 展示，增加供应商、实际模型、输入/输出/总 Token、耗时和用户标识；请求 ID 仅保留为内部调用链下钻键。新增只读 PostgreSQL 集成测试，不修改用量表结构和历史用量数据。

- 2026-07-21：精简“操作审计”列表展示，移除“变更前、变更后”两列，避免大段结构化内容占用表格空间；点击记录后仍在记录详情中完整展示变更前后内容。仅调整 Console 展示，不修改审计接口、审计数据或排序逻辑。

- 2026-07-21：统一所有列表的稳定记录顺序。通用管理列表默认按不可变 `id` 升序展示，用户主动选择其他排序字段时，同值记录仍以 ID 作为稳定次级排序；编辑状态、刷新或重新加载后，不再因接口返回顺序、更新时间或对象字段顺序变化导致记录跳位。调用日志保留按时间倒序，使用请求 ID 作为稳定次级排序；路由策略、内部成本单及明细、租户工作台、开发者模型列表、控制台告警/渠道摘要和首页渠道摘要同步采用稳定排序。列表列顺序继续由各页面显式字段配置决定，不依赖接口对象属性顺序。本次仅调整 Console 排序逻辑，不修改接口、数据库或业务数据。

- 2026-07-21：修复供应商官方价格“连续确认次数”无法可靠累计的问题。新增 Flyway V15，在 `provider_price_diff` 独立保存 `confirmation_count`、`last_confirmed_hash`、`last_confirmed_at`；同一标准化价格在不同同步任务中重复出现时累计次数，价格内容变化时重置为 1，同一任务重复执行不重复计数。自动发布改为直接读取独立确认状态，不再通过被覆盖的 `sync_run_id` 推断。价格差异审核列表新增“连续确认”进度，详情展示已确认次数、要求次数和最近确认时间，并新增集成测试覆盖“变化重置、连续两次一致后自动发布”。

- 2026-07-21：统一全站表格/列表类页面的滚动交互。通用数据页默认启用桌面端内部滚动，因此模型配置、租户与应用、成本管理、高级治理等使用通用列表的页面统一固定标题、筛选区与分页，仅表格区域滚动；路由策略、内部成本单、租户工作台和开发者服务模型列表等独立页面同步接入共享内部滚动布局。调用日志、用量分析沿用已实现的内部滚动。移动端继续使用自然页面滚动。本次仅调整 Console 布局样式，不修改接口、查询逻辑或业务数据。

- 2026-07-21：精简“告警事件”列表展示，移除“详情”列，避免长文本占用表格空间；点击记录后仍在记录详情中展示完整告警详情。仅调整 Console 展示，不修改告警接口、处理动作或业务数据。

- 2026-07-21：修复“供应商官方价格目录”输入金额/千 Token、输出金额/千 Token 显示为空的问题。根因是 Console 将后端 snake_case 字段转为 camelCase 时只处理下划线后的字母，`input_amount_per_1k`、`output_amount_per_1k` 被错误转换为 `inputAmountPer_1k`、`outputAmountPer_1k`，与页面配置字段不一致。现转换逻辑同时支持下划线后的数字，正确生成 `inputAmountPer1k`、`outputAmountPer1k`；不修改数据库价格和后端接口。

- 2026-07-21：统一“日常运营”菜单下页面的滚动方式。API Key、调用日志、用量分析、告警事件、渠道健康在桌面端均改为固定页面高度，标题、筛选区和分页保持可见，仅列表区域内部滚动，表头在滚动时保持固定；移动端继续使用自然页面滚动。此次仅调整 Console 布局样式，不修改接口、查询逻辑或业务数据。

- 2026-07-21：优化“操作审计”页面滚动方式。通用数据页新增可选的列表内部滚动模式，仅在操作审计启用；桌面端固定页面标题、筛选区和分页区域，审计表格在剩余高度内独立滚动，并固定表头。其他通用数据页面及移动端布局保持原有行为。本次仅调整 Console 展示，不修改接口、查询逻辑或审计数据。

- 2026-07-21：优化价格类页面的“列表精简、详情完整”展示策略。价格源管理列表继续隐藏价格源类别、适配器、认证方式、认证供应商渠道、低风险自动发布、最近成功时间、最近失败时间和最近错误，但记录详情保留这些字段；供应商官方价格目录列表隐藏来源可信度、目录版本和来源依据，记录详情继续展示。通用数据页新增独立 `detailFields` 配置，避免详情被列表字段裁剪影响。本次仅调整 Console 展示，不修改接口、表单、数据库或业务数据。

- 2026-07-21：精简“价格源管理”列表字段，移除低频技术字段“价格源类别、适配器、认证方式、认证供应商渠道、低风险自动发布”，并同步移除“最近成功时间、最近失败时间、最近错误”三列。底层字段及新建/编辑表单能力保留，列表仅聚焦价格源名称、供应商类型、来源地址、区域、币种、同步周期、自动变动阈值、确认次数、状态、待审核差异和更新时间。

- 2026-07-21：调整 API Key 页面说明文案为单行显示。通用数据页新增可选的 `descNoWrap` 展示能力，仅在 API Key 页面启用；宽度不足时使用省略号，不换行且不撑破页面布局。其他页面说明文案保持原有换行行为。

- 2026-07-21：移除“调用日志”和“用量分析”页面标题上方无业务必要的“观测中心”文案，仅保留页面标题、说明和刷新操作；不调整菜单、接口、查询逻辑或业务数据。

- 2026-07-21：修复 Console 时间显示比本地时间少 8 小时的问题。根因是统一 `formatDateTime` 只截取 ISO 时间中的年月日和时分秒，直接丢弃 `Z`/`+00:00` 时区信息，没有转换为浏览器本地时区。现改为：带时区的 ISO 时间先解析为绝对时间，再按浏览器本地时区显示为 `YYYY-MM-DD HH:mm:ss.SSS`；不带时区的时间保持原始时钟值，避免二次偏移。调用日志、用量分析、成本单、租户工作台和通用数据页等所有统一使用该格式化函数的页面同步生效。本次仅修改前端时间展示，不修改数据库时间、后端时区或业务数据。

- 2026-07-17：新增 `scripts/dev/chat-with-virtual-key.py`，用于通过 TokenSea Virtual Key 进行连续多轮对话。脚本启动时检查 Gateway、模型访问范围并自动携带历史上下文，支持清空、查看、保存和退出会话，可配置系统提示词、温度、最大输出长度、超时和上下文轮数；会话文件不保存密钥，并复用现有验证脚本的统一错误解析。

- 2026-07-17：按平台管理员真实使用频率和端到端操作依赖重构 Console 左侧菜单，并新增 `docs/TokenSea_管理员菜单与操作流程优化方案_V1.0_20260717.md`。管理员菜单由原来的“模型中心、同步中心、Key 中心、租户中心、成本与预算、路由中心、观测中心、安全与审计、系统设置”九组技术模块，调整为“日常运营、模型配置、租户与应用、成本管理、高级治理”五组业务入口；“日常运营”默认展开，集中 API Key、调用日志、用量、告警和渠道健康；“模型配置”按供应商渠道→模型部署→模型生效价格→企业服务模型→路由策略排列。供应商模板、公共参考库、能力验证、模型发现、同步任务、原始快照、模型差异审核、申请审批、发布记录、敏感查看和错误码中心从主菜单下沉但路由与功能保留。工作台快捷入口同步改为 Key、企业服务模型、路由和调用日志，渠道健康文案改为与当前可配置有效期一致。此次仅调整前端菜单、工作台和文档，不删除后端能力、不修改业务数据、不重启服务。

- 2026-07-17：完成 Gateway 39212 安全重启并清理多套残留 Uvicorn/`multiprocessing.spawn` 进程树。确认此前 `TOKENSEA_RUNTIME_CONFIG_FAILED` 的真实根因是本地 Gateway 继承系统 SOCKS 代理，导致 Gateway→Runtime Core 内部 `httpx` 请求在建立连接前报缺少 `socksio`；现已将健康检查、动态模型注册、普通/流式调用及重试统一设置 `trust_env=false`，内部通信不再经过系统代理。Readiness 端点同步改为明确返回 `configuration/database/redis/runtime-core` 失败位置。重启后 `/health` 与 `/health/readiness` 均返回 200，readiness=`ready`；数据库、Redis、Runtime Core、Control Plane、Console 均未重启。

- 2026-07-17：定位并修复 Virtual Key 真实调用返回 `TOKENSEA_RUNTIME_CONFIG_FAILED`。Gateway 日志确认根因是本地进程继承系统 SOCKS 代理，`httpx` 在访问本机 Runtime Core 前尝试初始化 SOCKS 传输，但环境未安装 `socksio`，导致动态模型注册在请求发出前失败；DeepSeek 未收到调用。所有 Gateway→Runtime Core 的内部 HTTP 客户端现统一设置 `trust_env=false`，明确绕过系统代理，覆盖健康检查、动态模型注册、非流式调用、流式调用及模型缺失重试。HTTPException 响应同时补充 `x-request-id`，验证脚本可显示请求 ID。Gateway 回归测试 27/27 通过，Python 语法检查通过。当前运行中的 Uvicorn worker 未自动加载最新代码，尚未重启 Gateway。

- 2026-07-17：修复真实 Virtual Key 调用中频繁要求供应商连接测试、DeepSeek 公网 DNS 轮换误报 503，以及后续通用 502 无法定位的问题。确认 Gateway 和企业服务模型发布均将连接测试有效期硬编码为 30 分钟，Gateway 还要求当前 DNS IP 集合与连接测试快照完全一致；DeepSeek `api.deepseek.com` 连接测试保存 8 个公网地址，而当前正常解析为其中 4 个，属于 CDN/多线路正常轮换。现将连接测试有效期默认调整为 7 天并支持 `TOKENSEA_CONNECTION_TEST_VALID_MINUTES`、`TOKENSEA_CONNECTION_TEST_MAX_AGE_SECONDS` 配置；Gateway DNS 校验改为严格保持域名和端口不变、当前解析必须全部为公网地址，公网 IP 集合正常增减自动接受，私网/回环/链路本地/保留地址继续拒绝；新增按“渠道 + 域名 + 端口”的跨请求 DNS 缓存，默认 300 秒，避免每次调用重新解析。Egress Proxy 的精确域名白名单和公网地址校验保持不变。两次失败请求 ID 分别为 `414c536a64684d2fb2938f8783d7440a`（DNS 503）和 `39c7140887a745b4a1b176c58f428efa`（Gateway 内部 502），均只生成 request intent、未生成 request attempt，证明未进入 Runtime Core/DeepSeek；Gateway 现在为未捕获异常和动态模型准备失败记录请求 ID、阶段和异常堆栈，并在响应头返回 `x-request-id`，验证脚本同步显示请求 ID。Gateway 回归测试 27/27 通过，JDK 21 `AdminFlowSimplificationTests` 7/7、`Phase1GContractTests` 8/8 通过，Python 语法检查和 Compose 配置解析通过。本次未重启运行服务。

- 2026-07-17：修复新建 Virtual Key 显示“启用”但 `/v1/models` 返回空列表的问题。确认问题数据为 Key 模型范围 `["deepseek-v4-flash-test"]`、租户 `wushengjin` 模型范围 `["deepseek-v4-flash"]`，同时验证命令使用了不存在于 Key 范围内的 `deepseek-v4-pro`，导致 Gateway 的“Key 范围 ∩ 租户范围”为空。Control Plane 创建 Key 时现在强制校验 Key 模型范围必须是租户模型范围的子集，超出时返回结构化错误 `KEY_MODEL_SCOPE_EXCEEDS_TENANT`，明确异常模型和处理方式，避免继续生成不可用 Key；Key 页面补充模型范围约束说明。JDK 21 定向测试 7/7 通过。本次未自动扩大现有租户模型权限，也未修改已生成 Key 的权限数据。

- 2026-07-17：进一步简化路由“校验并生效”流程。确认 `deepseek-v4-flash` 候选部署已审核但没有任何 `LIVE_PROBE` 记录，价格版本、币种和渠道基础状态均正常。路由生效现在会自动检查每个候选部署；缺少成功主动探测时自动执行一次 `CHAT` 真实探测，成功后自动更新部署为 `APPROVED + ELIGIBLE` 并继续完成路由生效，失败时返回具体渠道、模型、HTTP 状态、上游错误码、失败原因和处理方式，同时保留验证证据与审计记录。抽取 `CapabilityProbeService` 供能力验证页面与路由生效共用，避免重复逻辑。另修复供应商连接测试成功后仍残留历史失败文本的问题，成功结果现在会清空 `lastConnectionTestError`。JDK 21 定向回归测试 15/15 通过；本次未重启运行中的 Control Plane，也未直接修改路由、部署或能力验证业务数据。

- 2026-07-16：修复路由策略“校验并生效”仅弹出 `Conflict`：确认页面已加载新版按钮，但本地 Control Plane 仍是 16:13 启动的旧进程，早于 18:27 后完成的结构化异常代码，因此旧接口只返回裸 HTTP 409。Console 的错误解析新增 409 裸 `Conflict` 兜底，路由页面会显示“异常位置、异常原因、处理方式”，明确提示重启 Control Plane 加载最新代码；新版后端运行后继续优先显示真实的模型映射、能力验证、价格版本等具体校验问题。

- 2026-07-16：按“平台管理员为主要操作者、低风险自动放行、高风险才审批”简化核心流程。模型部署取消“先人工审核、后能力探测”的倒置流程，允许直接发起真实 LIVE_PROBE，探测通过后自动审核并获得路由资格，失败则保存验证证据并返回明确处理建议；路由策略由“提交审批→审批通过→审批后生效”改为管理员直接“校验并生效”，仍完整校验企业服务模型映射、候选部署、能力验证和价格版本并写入审计；企业服务模型在“需要审批=否”时管理员可直接发布，仅显式选择“需要审批”才进入审批中心；平台管理员新建 Virtual Key 自动授权，页面只保留“生成密钥、禁用”，历史待审批 Key 在管理员生成时自动补授权。新增统一业务异常结构，返回错误码、异常位置、具体原因和处理方式，前端统一展示“异常位置；异常原因；处理方式”；首批覆盖能力探测、路由生效、企业服务模型发布和 Key 生成。价格差异、高风险价格发布、强预算阻断等高风险治理审批保持不变。新增 `AdminFlowSimplificationTests` 覆盖能力探测自动审核、管理员直达路由生效、模型发布、Key 自动授权与结构化错误响应。

- 2026-07-16：修复企业服务模型发布仅返回模糊 409 的问题。确认 `deepseek-v4-flash-test` 关联路由 `deepseek-v4-flash-route` 仍为 `DRAFT`，对应部署虽已审核但 `routing_status=INELIGIBLE`，且没有 `LIVE_PROBE` 能力验证记录；发布被拦截属于正确治理规则。企业服务模型列表新增“路由状态”，发布前前端会实时校验关联路由，未生效时明确提示先完成能力验证、路由提交审批、审批通过和生效；后端 409 细分为路由不存在、路由不属于当前模型、路由未生效，并返回具体路由名称和状态。新增定向测试验证列表返回关联路由状态。本次没有绕过能力验证和审批，也没有直接修改路由或模型业务状态。

- 2026-07-16：完成 `deepseek-v4-flash` 从官方价格生成到 Virtual Key 调用、用量和成本快照的完整 E2E 测试，并新增 `docs/testing/TokenSea_deepseek-v4-flash_端到端测试记录_20260716.md`、`docs/testing/deepseek-v4-flash-e2e-result.json`、`scripts/dev/run-flash-e2e.py` 和默认关闭的 `scripts/dev/mock-openai-upstream.py`。最终请求 `86f7d86fc1ef4da6a841162052f5b9ec` 成功，记录 12/8/20 Token、`PROVIDER_OFFICIAL` USD 成本 `0.00000392`、预算 `SETTLED` 和记账 `COMMITTED`。测试中修复：相同价格范围重复生成待审核差异、BigDecimal scale 导致相同价格误判变更、企业服务模型误用新版价格版本触发旧表外键 500、LiteLLM 持久化模型在 Gateway 重启后重复注册冲突、LiteLLM v1.91 模型查询兼容、本地进程继承旧 Runtime Engine Key、Windows 残留多套 Uvicorn 进程，以及 Runtime Core 本地测试主机 NO_PROXY 配置。新增本地测试上游安全开关和精确主机白名单，默认关闭，生产 SSRF/出口代理规则保持不变。Control Plane 定向测试 13 项无失败（1 项独立 PostgreSQL 测试按配置跳过），Gateway Runtime 27/27 通过。当前环境缺少真实 DeepSeek API Key，因此外部 `api.deepseek.com` 生产调用未验证；本次最终响应来自明确标识的本地 OpenAI 兼容测试上游。

- 2026-07-16：新增 `docs/TokenSea_价格到VirtualKey端到端测试操作流程_V1.0_20260716.md`，基于当前平台真实菜单与数据状态，整理从 DeepSeek 官方价格源、同步任务、价格差异审核、供应商官方价格目录、模型生效价格、模型部署与能力验证、企业服务模型、路由策略、租户/项目/应用、Virtual Key 审批与生成，到 `/v1/models`、`/v1/chat/completions`、调用日志、用量、成本和审计核验的完整操作流程；明确当前 USD 官方价格与 CNY 预算基准币种的路由生效风险，并引用现有 PowerShell/Python Key 验证脚本。

- 2026-07-15：修复供应商连接测试错误将主机名写入 DNS 地址快照，导致 Gateway 实际调用报“供应商 DNS 快照无效”：连接测试现在解析并保存目标主机的公网 IP 列表，拒绝无法解析或包含本地、链路、站点、组播地址的结果；Gateway 继续以主机、地址快照和当前解析结果三重校验。

- 2026-07-15：修复企业服务模型的可见范围为“全部租户”时，经 Console 多选序列化为 `["全部租户"]` 却被 Gateway 误判为不可见的问题；Gateway 现在同时识别字符串和列表形式的“全部租户、内部租户、ALL、*”范围，并增加单元测试覆盖列表形式。

- 2026-07-15：修复路由策略审批流程未闭环导致企业服务模型发布 409：路由提交审批后现在将状态从 `DRAFT` 更新为 `PENDING_APPROVAL`，审批通过后仅允许“待审批”路由执行生效，生效后转为 `ACTIVE`；修正了页面无法出现“审批后生效”操作、已审批路由仍被视为草稿的问题。

- 2026-07-15：修复企业服务模型“新建草稿”按钮未渲染的根因：Vue 会将未传入的可选 Boolean Prop 解析为 `false`，导致通用数据页误将未显式禁用的创建和编辑能力关闭；现在为 `allowCreate` 和 `allowEdit` 设置符合原设计的默认值 `true`，只读资源及显式配置为 `false` 的页面仍保持禁用。

- 2026-07-15：明确企业服务模型的草稿入口：通用数据页支持按资源配置创建按钮文案，企业服务模型页的“新建”改为“新建草稿”，与先建模型草稿、再配置路由并发布的治理流程一致；其他资源页仍显示“新建”。

- 2026-07-15：解除企业服务模型与路由策略的新建循环依赖：Console 允许先保存未绑定路由的企业服务模型草稿，再以该服务模型创建并生效路由，最后回填路由并发布；字段标注为“路由策略（发布前必填）”，后端发布时的生效路由、渠道、能力和可见范围校验保持不变。

- 2026-07-15：修复混合部署下 Gateway 访问 `/v1/models` 返回“运行时配置不完整”：本地环境加载脚本按 Compose 的同一规则，将 `.env` 中的 `TOKENSEA_RUNTIME_CORE_KEY` 映射为应用进程使用的 `TOKENSEA_RUNTIME_ENGINE_KEY`，同时保留显式配置的 Engine Key 优先级；未生成、更改或输出任何密钥。

- 2026-07-15：修复路由策略价格版本下拉框为空及模型生效价格金额展示不稳定：确认 DeepSeek 官方价格解析、价格目录、`PROVIDER_OFFICIAL` 生效版本和实时 API 均已保存并返回 `deepseek-v4-pro` 的 USD 输入/输出金额；路由策略不再仅过滤 `CHANNEL_ACTUAL`，改为同时支持 `PROVIDER_OFFICIAL` 和 `CHANNEL_ACTUAL`，按供应商渠道与实际模型精确匹配，并在下拉项展示版本、币种及输入/输出金额。`/api/price-versions` 增加 `status` 筛选并显式返回 camelCase 价格字段，新增只读实时接口测试验证金额非空、层级和状态。Console 构建通过，Control Plane JDK 21 打包并重启，实时接口测试通过。另识别出当前预算基准币种 CNY 与 DeepSeek 官方 USD 价格之间的后续路由生效风险，以及当前数据库尚未创建名为 `deepseek-v4-pro` 的企业服务模型；本次未擅自修改预算币种、汇率或业务模型数据。

- 2026-07-15：新增 `scripts/dev/verify-virtual-key.py` 与 Windows PowerShell 版本 `scripts/dev/verify-virtual-key.ps1`，按“Gateway 健康检查、Virtual Key 鉴权与模型范围、真实对话调用”三步验证生产 Virtual Key；密钥仅从安全输入、命令参数或环境变量读取，不内置、不打印、不落盘，并区分 Key 无效、模型范围不足和真实调用失败。

- 2026-07-15：定位并修复价格差异“批准发布”独有失败：审批发布后自动匹配模型部署、生成 `PROVIDER_OFFICIAL` 价格版本时，`price_version` 插入 SQL 比字段和参数多一个占位符，触发后端异常；该异常在 `/error` 二次分发时又被 Spring Security 转成空正文 403，前端因此误报为管理员权限不足。已修正 SQL 占位符数量，并允许 ERROR 分发和 `/error` 返回真实业务异常，避免后续服务器错误再次伪装为 403。集成测试新增真实模型部署自动匹配断言，在独立临时 PostgreSQL 数据库完成 V1～V14 迁移后验证价格目录、价格组件及价格版本生成成功；安全与 JWT 定向测试 6/6 通过。

- 2026-07-15：继续修复价格差异审批 403：JWT 认证过滤器兼容旧令牌以用户名作为 `sub`、新令牌以用户 ID 作为 `sub` 的两种格式，先解析为数据库中的规范用户 ID，再加载实时角色和租户范围；避免旧管理员令牌声明含 `ADMIN`，但服务端按用户名误查用户 ID、导致审批控制器二次授权失败。新增 `JwtAuthFilterTests`，覆盖旧用户名主体解析和无效用户返回 401。

- 2026-07-15：进一步收敛价格差异审批授权路径：将所有 `/api/provider-price-diffs/**` 的 POST 请求交由控制器执行数据库实时平台管理员校验，避免单条路径匹配差异再次落入通用租户/管理员规则；补充批准、驳回、只读列表及其他管理接口的安全回归测试，4 项全部通过。

- 2026-07-15：修复价格差异审核“批准发布/驳回”被通用 `/api/**` 平台角色规则提前拦截的问题：SecurityConfig 对两个审批 POST 路径仅校验有效登录，再由 ProviderPriceSyncController 按数据库实时 `ADMIN` 角色执行二次授权，避免旧 JWT 角色声明或角色格式差异导致请求在进入业务控制器前返回空正文 403；同时将前端无正文 403 的兜底提示从“无权访问租户范围”改为明确的管理员授权提示。

- 2026-07-15：恢复 Control Plane 通用 API 路径授权规则，并在 JWT 认证过滤器中从数据库实时加载用户角色和有效租户范围后再授予 Spring Security 权限；避免特定路径匹配影响登录接口，同时保证价格差异审批仍以实时平台管理员角色授权。

- 2026-07-15：修复价格差异审核人乱码：确认当前管理员 `display_name` 数据已被写成问号占位符，已将本地管理员显示名称修复为“平台管理员”；列表和详情在显示名称为空或仅包含乱码占位符时自动回退为账号名称，并使用编码无关的 Unicode 字符构造“系统”，避免继续展示“?????”或编译编码造成的乱码。

- 2026-07-15：修正价格差异审批接口的 Spring Security 路径匹配范围为 `/api/provider-price-diffs/**` 的 POST 请求，确保请求可进入控制器执行数据库实时平台管理员校验，而非被通用 `hasRole` 规则提前拒绝。

- 2026-07-15：优化价格差异审核列表：移除“审核意见”列；审核人由内部用户 ID 改为用户显示名称，系统自动发布记录显示为“系统”，详情接口同步返回审核人名称。

- 2026-07-15：强化价格差异审批授权：批准发布、驳回接口先校验有效 JWT，再按数据库中的实时平台管理员角色二次校验；仅这两个审批接口不再依赖 JWT 内的角色格式，普通租户账号仍会被拒绝，解决管理员会话发生角色声明兼容差异时审批返回 403 的问题。

- 2026-07-15：价格同步任务页面补齐任务状态中文映射，列表、状态筛选和执行详情统一显示“待执行、执行中、执行成功、无价格变化、待审核、执行失败、已取消”，后端状态码保持不变。

- 2026-07-15：修复 JWT 角色声明的兼容性：Control Plane 同时识别数组和字符串形式的 `roles`、`tenant_ids` 声明，避免前端已识别管理员而服务端未授予 `ROLE_ADMIN`、导致价格差异审批 POST 返回 403。

- 2026-07-15：修复产品首页登录后控制台顶部会话未刷新的问题：外层布局改为响应式读取当前令牌，并在路由切换时刷新账号、角色、视图和菜单；管理员登录后正确显示账号缩写与“全局视图”，不再沿用登录前的 `TS / 租户视图` 状态。

- 2026-07-15：修复研发阶段 Console 跨源审批请求的本机回环 CORS 白名单：本地 Control Plane 启动时同时允许 `localhost`、`127.0.0.1` 和 IPv6 回环地址的 39210 Console 来源，解决从不同本机回环地址访问 Console 时价格差异审批 POST 预检返回 403 的问题；不放宽外部域名来源。

- 2026-07-15：修复价格差异审核的“查看差异”详情渲染：单条详情接口返回对象时统一包装为表格记录，确保旧价格、新价格和发布目录记录可见；“批准发布”“驳回”仅对待审核记录展示，避免对已处理记录发起无效审核请求。仅修改 Console 展示与交互，未变更价格数据或审批权限。

- 2026-07-15：统一前端时间显示格式，将 ISO 8601 字符串（如 `2026-07-15T01:34:39.299+00:00`）展示为 `2026-07-15 01:34:39.299`；覆盖通用数据页、调用/用量页、成本账单页和租户工作台，保留原时间值及其时区语义，仅调整展示格式。

- 2026-07-15：优化通用数据表格的长地址与操作列布局：URL/Endpoint 类字段固定为 280px 并允许按字符换行，最多显示三行且保留悬停完整地址；操作列固定为 220px，按钮按完整文字自适应并在列内换行，解决价格源管理页面横向过长和操作按钮文字被挤压的问题。仅修改 Console 展示。

- 2026-07-14：统一移除通用数据页面左上角无业务含义的“控制面数据”首行标签，保留页面标题、说明和操作区，减少页面头部纵向占用；仅修改 Console 展示，不涉及后端接口或数据结构。

- 2026-07-14：新增 `docs/design/TokenSea_研发阶段本地进程与Docker混合部署方案_V1.0_20260714.md`，明确研发阶段采用“PostgreSQL、Redis、LiteLLM、出口代理使用 Docker，Console、Control Plane、Gateway 使用本地进程”的混合部署模式，并给出 Windows 环境变量、启动命令、网络关系、开发脚本、测试与预发布验证流程；本次仅新增文档，未修改运行代码或正式部署配置。

- 2026-07-14：完成价格自动获取与自动更新闭环，新增 V14 迁移、价格源/同步任务/原始快照/公共价格参考/价格差异/价格组件数据结构，落地 LiteLLM、models.dev、DeepSeek 官方价格页及通用官方 JSON/CSV 适配器，支持托管渠道凭据认证、定时同步、连续一致确认、低风险自动发布、高风险审核、模型部署自动匹配、动态出口域名和请求级不可变成本组件快照；新增实现说明 `docs/design/TokenSea_价格自动获取与自动更新完整实现说明_V1.0_20260714.md`。已通过 JDK 21 编译、控制面定向测试、PostgreSQL 16 V1～V14 集成测试、Gateway/出口代理测试、Console 构建和 Compose 配置检查；本次未部署或重建正式 TokenSea Compose 环境。
- 2026-07-14：新增设计文档 `docs/design/TokenSea_LiteLLM与CCSwitch成本价格体系分析及落地方案_V1.0_20260714.md`，分析 LiteLLM 与 CC Switch 的成本页面、价格数据结构、原始来源、更新方式和请求成本计算，明确 TokenSea 采用“LiteLLM 计费知识覆盖 + CC Switch 页面体验 + 企业级数据库价格治理”的组合方案；本次仅更新文档，未修改代码、数据库或部署配置。
- 2026-07-14：新增设计文档 `docs/design/TokenSea_自动获取供应商官方API价格方案_V1.0_20260714.md`，明确采用官方机器可读价格 API、官方价格页面专用适配器和受控离线导入的分级同步方案，包含原始快照、差异审核、低风险自动发布、动态出口规则及与现有数据库价格目录的衔接设计；本次仅更新文档，未修改代码、数据库或部署配置。
- 2026-07-14：价格治理改为数据库驱动：新增供应商官方价格目录、模型发现自动匹配、自动生成 `PROVIDER_OFFICIAL` 生效版本、价格缺失告警和请求级价格来源快照；控制台新增“供应商官方价格目录”，原“成本价格”调整为只读“模型生效价格”，企业服务模型不再要求人工绑定价格版本。
- 2026-07-14：新增完整设计文档 `docs/design/TokenSea_供应商官方价格数据库化与自动匹配方案_V1.0.md` 和前向迁移 `V13__provider_official_price_catalog.sql`；未修改 V1–V12 已发布迁移。

- 2026-07-13：修复供应商渠道连接测试路径参数解析：明确声明 `id` 参数名，避免编译产物未保留参数名时路径解析失败。
- 2026-07-13：供应商连接失败作为受控业务结果返回，前端直接展示出口白名单、密钥或上游失败原因，不再将可预期的测试失败误呈现为权限 403。
- 2026-07-14：修复供应商模型发现接口的路径参数解析；发现失败改为受控业务响应，避免管理员操作被误显示为租户权限不足。
- 2026-07-14：控制面 Java 编译保留接口参数名，修复模型部署、能力验证、成本价格等治理页面的查询参数解析异常，避免被错误显示为租户权限不足。
- 2026-07-14：修复治理查询在未传筛选条件时的 PostgreSQL 空参数类型推断失败；模型部署、能力验证、成本价格、告警及治理列表不再因数据库异常被误显示为权限不足。

- 文档日期：2026-07-14
- 依据：`TokenSea_PDD_V1.4_20260710.md` 与 `TokenSea_Product_Prototype_V1.9_PDD_V1.4`
- 交付范围：TokenSea 控制面、Gateway Runtime、出口代理、Console、Compose 部署契约及 V5–V12 数据迁移
- 说明：本记录只陈述实际完成和实际执行的检查；未执行或受环境限制的项目单列说明。

## 1. PDD 范围与交付结论

本轮完成了模型供应商与渠道、公共模型参考、企业服务模型、真实模型发现与差异审核、主动能力探测、价格版本、路由、Virtual Key、租户/项目/应用、预算、实际成本、供应商对账、调用追踪、告警、审批与版本回滚、审计、开发者快速开始等核心闭环。

交付实现遵循以下原则：

- 页面业务选项使用中文展示，下拉选项由受控枚举或真实后端数据源提供。
- 业务页面不展示“MVP”等开发阶段文字。
- 运行数据来自数据库、供应商接口、Gateway 或受控导入，不以静态演示数据冒充真实状态。
- 高风险发布、激活、预算生效和回滚进入审批与审计链路。
- Gateway 只使用通过主动探测、审核和价格/预算治理的可路由配置。

## 2. 版本修订摘要

### 登录前产品首页（2026-07-13）

- 新增根路径 `/` 的 TokenSea 产品首页，对齐 V1.7 原型的顶部导航、首屏说明、控制台预览和接入生态布局。
- “进入控制台”“查看控制台”“快速接入”和“登录”均连接到现有控制台或认证路径；未登录访问受保护页面仍会进入登录页。
- 预览卡片只读取控制面实际聚合指标与供应商渠道；接口不可用或没有记录时显示明确状态，不以原型示例指标充数。
- 首页移除首屏和右上角的控制台入口；右上角登录改为弹出式表单，认证成功后进入其权限对应的工作台。
- 登录成功后保留产品首页；用户通过顶部“控制台”进入工作台，控制台退出登录后返回产品首页。
- 登录态仅展示账号缩写；首页与工作台均通过点击缩写打开“退出登录”菜单，避免在顶部直接暴露退出按钮。
- 退出后，产品首页忽略控制台卸载阶段迟到的未授权响应，避免被全局认证拦截器再次跳转到登录页。
- 产品首页的“控制台”入口在未登录时复用登录弹窗；仅已登录用户点击后进入工作台。
- 工作台标题区和概览区域操作按钮统一为更清晰的描边/主操作层级、悬停反馈和加载禁用态；右上角账号缩写增加边框与悬停反馈。
- 供应商渠道连接测试的 403 提示按会话角色区分：无 `ADMIN` 角色提示重新登录，避免将管理员角色缺失误报为租户范围无权。
- 修复供应商渠道连接测试的路径参数解析：明确声明 `id` 参数名，避免编译产物未保留参数名时被安全层错误转换为 403。

### 控制面与数据治理

- 增加真实供应商连接验证、DNS/出口目标约束、模型目录发现、原始快照、差异审核和字段来源。
- 增加服务端 CHAT、STREAM、EMBEDDING 主动能力探测；客户端不能自行提交通过结果。
- 企业服务模型、路由策略、价格版本和预算规则增加提交、审批、激活、退役、版本与回滚链路。
- 价格严格区分公共参考价、渠道实际成本和内部核算价，并校验层级、归属对象、来源类型与依据。
- 数据源同步支持 Provider API、HTTPS 白名单公共参考源和最大 2 MB/5000 条的受控 JSON/CSV 导入；定时任务具备抢占、心跳、日志和失败隔离。
- 增加真实错误码注册表、系统设置持久化与脱敏、敏感访问记录、快照安全查看和动态快速开始配置。

### Gateway、成本与预算

- 路由候选要求部署已审核、可路由且存在成功的 `LIVE_PROBE`。
- Gateway 优先读取已生效的渠道实际价格版本，并记录逐请求成本快照和多次调用尝试。
- 预算规则按租户、项目、应用和 Virtual Key 执行；仅执行 `ACTIVE + APPROVED` 规则，支持阻断、告警和降级。
- 增加成本单实体与多维明细、生成/确认/调整/导出状态机；生成时识别缺失价格、对账偏差、预算事件和失败尝试并标记异常。
- 供应商对账按真实 usage/cost 与账单输入拆分 Token、价格、汇率和税费差异，支持确认、解决、审计和导出。

### 安全与审计

- Provider 请求强制经独立出口代理、主机/端口 allowlist、DNS 快照及响应大小限制。
- 密钥使用托管密文，不通过运行时配置或 API 返回明文。
- `AuditService` 自动记录 actor ID、actor name、来源 IP 和 User-Agent。
- 原始模型快照查看必须提供理由，敏感字段受控脱敏并写入敏感访问日志。
- Compose 中 PostgreSQL、Redis、控制面、Gateway、Runtime Core、Console、Prometheus 和 Grafana 均限制为 `127.0.0.1` 发布端口。
- Redis 配置密码环境变量及 `requirepass`；Gateway outbox 使用持久卷；内部网络标记为 `internal: true`，出口代理单独连接 provider-egress 网络。

### Console/UI

- 按 PDD 重组模型资产、同步中心、访问治理、租户体系、成本预算、路由观测、审计设置和开发者中心导航。
- 增加公共模型参考、模型部署、能力验证、价格版本、预算、审批、版本回滚、调用详情、成本单、开发者模型、快速开始和租户工作台页面。
- 表单中的状态、类型、币种、动作、来源等使用中文下拉选项；租户、项目、应用、渠道、模型等选项读取真实 API。
- 移除不可用的静态 API 文档/监控占位页面及旧样式入口，统一真实空态、错误态和加载态。

## 3. 数据迁移与冻结

V5–V12 是已验证的前向迁移链。交付后不得修改已发布迁移；后续数据库变化必须新增 V13 或更高版本。以下 SHA-256 用于冻结核验：

| 迁移 | SHA-256 |
|---|---|
| V5 `mvp_runtime_security_compatibility` | `908d6acd01835f0077c1fb03e78708aca0e6a0a7cd96db9fa2b94302208b977f` |
| V6 `runtime_routing_accounting` | `a3f45b20cf97b0d354fc74fbb4a7f4320d0d2b78685c21ab2a1e950c3fd48cde` |
| V7 `control_plane_contracts` | `e68746c814e6269f4fa2629c1bbcc05fcbce9ab227ccf9051d4a4ffbf80fa612` |
| V8 `upgrade_recovery_and_accounting` | `320a17a0ec4f087b6bda95a9185e567d31cbecda89dd0df60bf385e864fb050e` |
| V9 `tenant_scope_and_egress_contract` | `f6e4ee549d7fdf9b6177aaa957767926474174bb3ee65a7db815c16f1d6e4cbb` |
| V10 `governance_discovery_and_cost_contracts` | `f336d415a6ce2d9e706c42ae0701cc3f9e4235a1c40aee951135565c99daede0` |
| V11 `runtime_governance_execution` | `70af1abe8054f4a44043a863b940eade7c2b7aa23d40490691fd300aeca9a78c` |
| V12 `pdd_delivery_closure` | `1023631bbd417e2911eb5a40ae45e57f682ef827f34cb923dd276ad0c7db262c` |

## 4. 实际通过的验证

### Java 与数据库

- JDK 21 `mvn -DskipTests package`：通过；84 个主源码和 5 个测试源码完成编译并生成可执行 JAR。
- JDK 21 全量 `mvn test`：19/19 通过，0 failure、0 error、0 skipped。
- `TokenseaApplicationTests`：2/2 通过；测试使用合法 Base64 32-byte 密钥和不可外联的本地测试代理。
- Flyway clean install：V1–V12 全部执行并验证成功，最终版本 V12。
- Flyway dirty upgrade：从含历史脏数据的 V6 快照升级至 V12 成功，隔离和兼容断言通过。

### Python/Gateway

- Gateway Runtime pytest：23 个测试通过，另有 10 个子测试通过。
- Egress Proxy pytest：23 个测试通过。

### Console

- npm/Vite production build：通过，包含 TypeScript/Vue 类型检查和生产资源构建。

## 5. 静态安全与依赖检查

### 已执行并通过

- 对 Git tracked/staged 内容执行常见秘密模式扫描；排除真实 `deploy/compose/.env`、`LOCAL_RUN.md` 和 gitignored 文件后，私钥、AWS Key、GitHub/OpenAI Token 和硬编码 secret 模式命中路径为 0。
- Maven runtime dependency inventory 成功生成：108 项依赖，108 项均存在本地 POM 元数据。
- `docker compose -p tokensea --env-file ./.env config --quiet` 成功解析；复审结果：
  - 所有发布端口均绑定 `127.0.0.1`。
  - `tokensea-internal` 为内部网络。
  - Gateway 只连接内部网络。
  - Egress Proxy 连接内部网络和独立 provider-egress 网络。
  - Gateway outbox 使用持久卷。
  - Redis 同时具备密码环境变量和 `requirepass`。

### 限制与未判定项

- `npm audit --omit=dev` 已实际执行，但当前 `npmmirror.com` audit API 返回 `404 NOT_IMPLEMENTED`；因此没有 npm 漏洞“通过”结论。
- Maven POM 许可证元数据自动汇总中有 34 项未声明或无法识别；本次只生成清单，没有作法律/许可证兼容性结论。
- 常见秘密模式扫描不是完整的熵分析、历史提交扫描或外部 secret scanner，结论仅覆盖本次扫描规则和当前 tracked/staged 内容。

## 6. Docker 运行态与限制

- 本轮没有执行 Docker image rebuild。
- 曾尝试 Docker build/全栈验证，但构建过程无输出并挂起，已终止；不得将其记为构建通过。
- 未执行完整 Compose 全栈启动后的端到端验收，因此没有“完整 Docker 全栈运行通过”结论。
- 为 Flyway clean/dirty 测试启动过一次性 PostgreSQL 16 测试容器；测试完成后已按严格名称校验删除，未删除卷、未清理或修改现有 TokenSea 数据库。
- Compose 本轮结论属于静态解析与安全配置审计，不等同于生产网络、宿主防火墙或运行时渗透测试。

## 7. 交付边界

- 未修改、未暂存用户的 `LOCAL_RUN.md` 工作树变更。
- 真实 `.env`、构建产物 `dist/`、`target/`、`node_modules/`、`__pycache__/` 和运行时 outbox 数据不进入交付暂存。
- 本记录不包含任何真实密钥、密码或 Token。
## 2026-07-13 部署权限回归修订

- 现象：控制面 `GET /api/model-templates` 返回 `403 Forbidden`。
- 根因：已存在的 `admin` 账号未关联 `ADMIN` 角色，登录 JWT 的 `roles` 为空；重新部署后的 `/api/**` 管理接口按角色拒绝访问。浏览器旧令牌也会继续复用这一无角色声明。
- 修复：使用部署端一次性恢复令牌为 `admin` 补授 `ADMIN` 角色并验证登录 JWT 已包含 `ADMIN`；前端对无角色令牌收到 403 时清理会话并跳转登录，避免旧令牌循环失败。
- 验证：重新登录后 `GET http://localhost:39211/api/model-templates` 返回 `200`。
- 安全补强：JWT 验签失败现在返回 `401`，前端可自动清理失效令牌并重新登录；普通租户账号访问该管理端点仍保持 `403`。
## 2026-07-13 租户表单枚举修订

- 新建和编辑租户弹框中的“租户类型”“状态”均改为下拉选择，选项显示为中文：内部租户、外部客户；草稿、启用、暂停。
- 新建租户默认选择“内部租户、草稿”；选择“启用”会沿用既有启用流程并生成默认访问密钥，不能直接新建“暂停”租户。
## 2026-07-13 原型壳层与菜单修订

- 仅调整全局控制台壳层和菜单，不改造各业务页面：使用原型的浅色顶栏、260px 可折叠侧栏、白色卡片、蓝色状态令牌与表格规范。
- 菜单分组与原型对齐为模型中心、同步中心、Key 中心、租户中心、成本与预算、路由中心、观测中心、安全与审计、系统设置、开发者门户；菜单仍只展示现有且有真实路由的功能项。
- 顶栏集群状态从控制面健康检查获得，不使用静态运行状态。
## 2026-07-13 首页原型重建

- 首页按原型的“运行总览、模型运行态、风险入口、快速下钻”布局重建。
- 原型中的演示 KPI、趋势、SLA、成本和待办未直接迁移；首页只使用 `/api/dashboard/stats`、供应商渠道和告警接口的真实数据。
- 无真实数据时显示空态，不使用静态业务占位内容。

## 2026-07-14 代码知识图谱文档

- 新增 `docs/TokenSea_代码知识图谱_20260714.md`，梳理前端、控制面、Gateway Runtime、出口代理、Flyway 迁移、核心数据关系与排查路径。
- 文档基于当前源码静态扫描生成，明确标注其不替代实际部署、供应商连通性和端到端调用验收。

## 2026-07-14 Compose 环境变量同步

- 将 `TOKENSEA_EGRESS_POLICY_TOKEN` 和 `TOKENSEA_EGRESS_POLICY_REFRESH_SECONDS` 同步加入本地 `deploy/compose/.env`。
- 出口策略令牌使用本地随机值保存，不写入文档、不输出到终端；刷新间隔与 `.env.example` 保持为 15 秒。

## 2026-07-14 研发阶段混合部署

- 新增 `deploy/compose/docker-compose.dev.yml`：研发模式将出口代理的动态策略地址指向宿主机本地 Control Plane，并发布本地进程访问所需的 18080 端口。
- 新增 `scripts/dev` 启动脚本：Docker 仅运行 PostgreSQL、Redis、出口代理和 Runtime Core；Console、Control Plane、Gateway Runtime 通过本地进程启动。
- 切换研发模式时移除同端口的 Docker 业务容器，但不删除数据库、Redis、Grafana 或 Gateway Outbox 数据卷。

## 2026-07-15 混合部署手动重启说明

- 新增 `docs/TokenSea_混合部署手动重启说明_20260715.md`，提供人工停止本地进程、启动 Docker 基础设施、启动三个本地服务、Flyway 与出口代理验证、完整 Docker 回退的操作步骤。

## 2026-07-15 Runtime Core 动态模型注册修订

- 现象：Virtual Key 已可读取 `deepseek-v4-pro`，但实际调用返回 `502 TOKENSEA_UPSTREAM_TIMEOUT`。
- 根因：Gateway 通过 LiteLLM `/model/new` 注册已批准部署；Runtime Core 未配置数据库，LiteLLM 返回 `No DB Connected`，Gateway 的 20 秒注册请求最终被归类为上游超时。
- 修复：Compose 为 Runtime Core 配置与控制面共用的 PostgreSQL 连接，并启用 `STORE_MODEL_IN_DB`，使动态模型注册可持久化并可供后续调用使用。
- 影响：首次重建 Runtime Core 时 LiteLLM 会在 TokenSea PostgreSQL 中创建自身所需的元数据表；不会删除现有 TokenSea 数据。

## 2026-07-16 Runtime Core 与模型列表修订

- Runtime Core 的 LiteLLM 元数据改用 PostgreSQL `litellm` 隔离 schema，避免 Prisma 基线迁移扫描或修改 TokenSea 控制面的 `public` schema。
- `/v1/models` 仅依据企业服务模型发布状态、已启用路由、租户可见范围与 Virtual Key 模型范围返回可访问模型；供应商最近一次连接测试过期不会再使已授权模型从模型列表消失。
- 实际调用仍会在路由执行阶段校验供应商连接测试、健康状态、DNS 快照与密钥，避免把失效渠道用于上游请求。

## 2026-07-16 数据库与登录恢复

- 现象：登录接口返回 HTTP 500；日志确认 `user_account`、Flyway 历史和多个 TokenSea 业务表在 `public` schema 中不存在。
- 根因：LiteLLM Runtime Core 曾连接 TokenSea 的 `public` schema 并执行 Prisma 迁移，覆盖了该 schema 的原有结构。
- 处置：将 LiteLLM 固定隔离到 `litellm` schema；清理错误遗留在 `public` 的 LiteLLM 表后，由 Flyway 重新执行 TokenSea V1–V14；从 2026-07-09 的只读旧卷副本恢复 `admin` 账号密码哈希，并补授 `ADMIN` 角色。
- 验证：Flyway V14 成功；Console、Control Plane、Gateway、Runtime Core 健康检查均返回 200；无效凭据访问登录接口返回业务失败响应而非 HTTP 500。
- 数据边界：旧卷只保存 V1 的 1 个账号，不含租户、供应商、模型、价格、路由或 Virtual Key；这些后续配置无法从现有卷恢复，需重新配置或从独立备份恢复。

## 2026-07-16 价格流程文档状态修订

- 数据库恢复后核验 `model_price` 与 `price_version` 均无记录；“供应商官方价格目录”和“模型生效价格”中金额为空是缺少价格数据的结果，不是页面金额字段映射异常。
- 更新 `TokenSea_价格到VirtualKey端到端测试操作流程_V1.0_20260716.md` 的环境状态：明确价格源、目录、模型生效价格、模型部署、路由和 Virtual Key 都需要重新创建，并保留 DeepSeek 官方价格的预期金额作为同步后的核对值。

## 2026-07-16 供应商渠道新建修订

- 现象：控制台已选择供应商模板后，新建供应商渠道仍返回 HTTP 400。
- 根因：前端表单仅提交供应商模板 ID，后端却额外要求不可见的 `providerType` 字段，导致合法表单无法通过校验。
- 修复：控制面根据选中的供应商模板自动读取并保存供应商类型；模板不存在或未配置类型时返回明确的中文错误信息。

## 2026-07-22 Docker Maven 构建网络兼容性修订

- 现象：Docker Desktop 重装后全量构建 Control Plane 镜像时，Maven Central TLS 握手中断。
- 修复：Control Plane 构建阶段设置 `MAVEN_OPTS=-Djava.net.preferIPv4Stack=true`，优先通过 IPv4 下载依赖。
- 修复：增加 Maven 公共镜像配置，构建时使用阿里云 Maven 公共仓库，避免 Maven Central TLS 握手不稳定。
- 影响：仅影响镜像构建阶段的依赖下载地址和网络协议栈选择，不改变 Control Plane 运行时逻辑。

## 2026-07-22 本地部署步骤文档

- 新增 `docs/TokenSea_本地部署步骤与大镜像手动下载说明_V1.0_20260722.md`。
- 内容：记录 Docker Desktop 代理修复、Maven 镜像切换、基础镜像手动下载、Gateway 启动顺序修复和部署完成检查方法。

## 2026-07-22 产品首页登录错误提示修复

- 现象：产品首页登录失败时，后端已返回“用户名或密码错误”，前端却提示“登录响应缺少访问令牌”。
- 修复：产品首页登录弹框先处理 `success=false` 业务响应，再读取访问令牌；失败时展示后端真实错误信息。
- 部署：仅修改前端源码，未执行 Docker 重建。

## 2026-07-22 研发混合部署预算币种配置修订

- 现象：Gateway 本地进程启动时因预算基准币种与当前 CNY 汇总实现不一致而退出，浏览器访问 39212 显示 Network Error。
- 修复：将本地 Compose 环境中的预算基准币种配置调整为 CNY，并使用混合部署启动流程重新加载 Gateway。

## 2026-07-22 本地管理员初始化恢复

- 核验：Docker Desktop 重装后的当前数据库用户数为 0，初始化状态未完成，不存在可重置密码的 `admin` 账号。
- 处置：通过受控首次初始化接口创建 `admin` 并授予平台管理员角色，使用本地运行文档中的默认凭据；未清空或覆盖任何现有业务数据。
- 验证：登录接口返回成功且包含访问令牌，数据库实时角色校验为 `ADMIN`。

## 2026-07-22 登录错误提示修订

- 现象：登录接口返回业务失败时，前端统一提示“登录响应缺少访问令牌”。
- 根因：登录页面未先判断接口的 `success` 字段，直接读取 `data.token`，掩盖了后端返回的真实错误信息。
- 修复：登录页面先处理业务失败响应，再兼容读取 `data.token` 或顶层 `token`。
- 验证：使用当前控制面接口确认无效凭据返回“用户名或密码错误”，前端将展示该业务错误而不是令牌缺失提示；未修改账号密码或数据库数据。

## 2026-07-22 汇率人工维护弹框布局修复

- 现象：人工维护月度汇率弹框中的目标币种和汇率金额输入框超出弹框右侧边界。
- 根因：全局输入框最小宽度与弹框内双列、带单位金额输入栅格的可用宽度冲突，CSS Grid 按最小内容宽度溢出。
- 修复：仅在汇率人工维护表单中允许输入控件在栅格内收缩，并将两列和金额输入的弹性列改为 `minmax(0, 1fr)`，保留现有桌面与移动端布局。

## 2026-07-22 供应商官方价格目录表单精简

- 移除新建、编辑供应商官方价格目录表单中与模型别名含义重复的“展示名称”输入项。
- 模型别名改为与其他字段一致的单行输入框，支持中文或英文逗号分隔多个别名；提交时自动转换为别名列表。
- 控制面在未传展示名称时自动采用首个模型别名，若没有别名则采用供应商模型名，保持历史数据和下游价格匹配兼容。

## 2026-07-22 Flyway 迁移版本冲突修复

- 现象：混合部署启动 Control Plane 时，Flyway 检测到“账户与角色管理”与已执行的“月度汇率与人民币汇总”同时使用 V17，服务无法启动。
- 修复：已执行的月度汇率迁移保持 V17 不变；将尚未执行的账户与角色管理迁移顺延为 V18，避免修改已发布迁移及已存在的汇率数据。

## 2026-07-23 价格源编辑表单修复

- 将价格源状态调整至表单前部并保留中文状态下拉选项，避免弹框滚动后提交时出现“请填写：状态”。
- 官方域名改为标准单行输入，支持中英文逗号分隔多个域名；编辑回填时正确展示已有域名列表。
- 适配器配置改为整行 JSON 编辑区，新增和编辑时均格式化对象值，避免显示 `[object Object]`；提交时继续校验 JSON 并按对象发送给控制面。

## 2026-07-23 价格源状态与紧凑表单修订

- 价格源新建与编辑表单不再提交或校验状态：新建由控制面默认创建为草稿，编辑由控制面保留原状态；状态仅通过启用、暂停等受控操作变更。
- 仅对价格源表单启用紧凑间距，JSON 配置输入区缩为三行，减少弹框高度并避免底部字段需要滚动才能查看。

## 2026-07-23 价格源编辑提交 400 修复

- 根因：价格源列表接口返回下划线字段和 PostgreSQL JSONB 对象，通用编辑表单回填时却直接读取驼峰字段，导致来源类别、适配器、认证方式和官方域名等关键值为空；提交后触发控制面参数校验并返回 HTTP 400。
- 修复：编辑回填统一使用通用的下划线字段映射，并解包 JSON/JSONB 值后再显示为域名列表或格式化 JSON；提交时可保留原有有效配置。

## 2026-07-23 DeepSeek 价格源压缩响应兼容

- 现象：DeepSeek 官方价格页同步时，响应中的压缩字节被直接按 UTF-8 保存到原始快照，PostgreSQL 拒绝其中的 `0x00`，价格源被标记为异常。
- 修复：价格同步服务按 `Content-Encoding` 解压 gzip、deflate 响应后再进行文本解析和快照写入；对不支持的编码返回明确错误，并限制解压后的响应最大为 5MB。
- 补充：对解压后的文本移除 NUL 控制字符，避免官方页面响应中的不可存储字符导致 PostgreSQL 文本快照写入失败。

## 2026-07-23 混合部署出口代理宿主机映射修复

- 现象：Docker Desktop DNS 配置更新后，研发模式出口代理无法解析 `host.docker.internal`，动态出口白名单刷新失败，官方价格源被误判为未授权。
- 修复：研发 Compose 为出口代理显式设置 `host.docker.internal:host-gateway`，使其可稳定访问宿主机的本地 Control Plane 动态策略接口。

## 2026-07-22 供应商渠道密钥操作文案调整

- 将供应商渠道操作“托管或轮换密钥”更名为“托管密钥”，并调整为操作列表第一项；接口、表单字段和现有密钥处理逻辑不变。

## 2026-07-22 供应商渠道操作按钮布局调整

- 供应商渠道操作列调整为三列网格：五个操作按钮按“三个 + 两个”显示为两行；仅扩大该页面的操作列，不影响其他数据页按钮布局。

## 2026-07-22 模型发现自动能力探测

- 模型发现成功后，后台会对新发现、重新出现或尚无探测记录的待验证模型自动执行一次 CHAT 真实探测；发现接口不等待探测完成。
- 探测成功时自动更新模型部署为“已审核 / 可路由”；失败会保留验证证据，后续模型发现不会重复自动调用，避免反复产生上游费用。
- 模型部署页增加“重新探测”和“验证记录”，用于处理失败项；不需要单独进入能力验证页面。

## 2026-07-22 路由候选价格版本下拉框溢出修复

- 路由策略候选链路的价格版本选项包含完整计费信息，长文本会使 Ant Design 下拉框撑破 Grid 列并覆盖优先级输入框。
- 为候选列和下拉框补充可收缩约束，选择项在输入框内省略显示，完整文本仍可在下拉选项中查看。

## 2026-07-22 路由候选外币官方价校验修复

- 路由候选校验错误地要求供应商官方价格币种必须等于平台预算基准币种 CNY，导致已生效的 USD 官方价被误判为缺少价格。
- 已改为接受任意原币种的已生效供应商官方价；预算和成本汇总仍由汇率换算链路统一折算为 CNY。

## 2026-07-22 默认 Virtual Key 模型范围同步修复

- 现象：租户已配置模型范围，但早期创建的默认 Virtual Key 仍为 `[]`，网关返回 `TOKENSEA_SCOPE_EMPTY`。
- 修复：默认 Key 创建时直接继承租户模型范围；对已启用租户执行“启用并同步默认 Key”会在不更换密钥的前提下更新该默认 Key 的范围，并写入审计记录。
- 约束：未配置模型范围的租户不能启用，避免生成无法调用的默认 Key。

## 2026-07-23 供应商官方价格目录状态校验修复

- 现象：新建官方价格目录时，状态字段未展示，前端却仍将其作为必填项，导致无法提交。
- 修复：移除该隐藏字段的前端必填校验；后端继续在未传状态时默认创建为“启用”。

## 2026-07-23 供应商官方价格目录时间提交修复

- 现象：价格目录的本地日期时间输入没有时区，提交到使用 `OffsetDateTime` 的接口时被 Spring 拒绝为 HTTP 400。
- 修复：通用数据表单提交前将日期时间输入转换为带时区的 ISO 时间戳，价格目录及其他同类表单均可被接口正确解析。

## 2026-07-24 顶部全局搜索移除

- 移除控制台顶部无明确全局检索能力的搜索框、跳转逻辑及其专用样式；各业务页面原有的列表筛选保持不变。

## 2026-07-24 供应商渠道操作按钮紧凑化

- 供应商渠道操作列总宽度从 190px 收紧至 176px；五个操作按钮按三列“3+2”布局为两行。
- 按钮不再使用额外横向内边距，字号为 12px，与表头字号一致，保证四字操作名称在窄按钮内完整显示。

## 2026-07-24 供应商渠道排序与长操作反馈

- 供应商渠道列表默认按 `createdAt` 降序显示。
- “连接测试”和“发现模型”请求执行期间，触发按钮显示旋转加载状态并禁止重复提交，直至服务端返回结果或错误。

## 2026-07-24 模型候选列表重叠修复

- 为模型候选表格配置模型名、状态、时间等关键列的稳定宽度，并允许该表在空间不足时横向滚动。
- 候选状态标签可在列内换行，避免覆盖“最近发现时间”和操作列内容。

## 2026-07-24 页面标题说明单行展示

- 统一扩大所有页面标题下说明文案的可用宽度；桌面端优先单行并在空间不足时省略显示。
- 移动端保留自然换行，避免窄屏内容被截断。
## 2026-07-24 页面说明与模型候选列冲突修正

- 模型候选列表移除与“当前可用性”重复的候选状态列，保留其在详情中展示，避免长状态标签侵入最近发现时间列。
- 页面标题说明区取消桌面端宽度上限；在可用宽度内单行省略展示，窄屏仍自动换行。

## 2026-07-24 模型候选表横向滚动移除

- 将“最近发现时间”列收窄至 96px，并移除模型候选表 1600px 的固定最小宽度；表格按页面可用宽度布局，不再显示横向滚动条。

## 2026-07-24 模型部署默认排序

- 模型部署列表默认按 `createdAt` 降序请求和展示，新创建的部署优先显示。

## 2026-07-24 价格源管理列表说明优化

- 加宽价格源名称列至 190px，减少名称换行。
- 为测试获取、测试解析、立即同步、启用、暂停按钮增加简短悬浮说明，明确每项操作的作用。

## 2026-07-24 价格源启停按钮合并

- 价格源管理将“启用”和“暂停”合并为状态切换按钮：当前启用时显示“停用”并调用暂停接口，其他状态显示“启用”并调用启用接口。

## 2026-07-24 渠道健康详情复用

- 渠道健康右侧详情抽屉复用供应商渠道的详情字段与“连接测试详情”区块，统一展示渠道配置、限流、密钥健康和最近连接测试信息。

## 2026-07-24 项目与应用新建默认启用及状态错误反馈

- 新建项目和应用默认写入“启用（ACTIVE）”状态，避免创建后还需单独执行启用操作。
- 控制面将状态冲突等业务异常统一返回具体原因；例如应用启用时上级租户或项目未启用，前端会直接展示该原因。

## 2026-07-27 生效价格版本组件复制修复

- 修复 PostgreSQL `jsonb` 通过 JDBC 返回 `PGobject` 时被错误识别为空价格组件的问题；官方价格目录自动匹配到模型部署后，会将解析出的完整组件数组写入新的生效价格版本。
- 已增加回归测试，覆盖 `PGobject` 形式的价格组件读取。历史空组件版本保持不可变，需在部署修复后通过“供应商官方价格”的“重新匹配”创建替代版本。

## 2026-07-27 新价格版本即时匹配状态修复

- 修复同一事务内创建官方生效价格后，使用 PostgreSQL 事务起始时间判断生效窗口而短暂误判“价格缺失”的问题；状态刷新改用实时 `clock_timestamp()`。
- 补充事务内自动匹配回归场景，确保新价格版本创建后模型部署立即标记为“已匹配官方价格”。生产路由恢复仍要求平台管理员显式确认进入生产。

## 2026-07-27 调用日志归因名称展示修复

- 调用日志改用用量分析明细接口读取数据，使租户、项目、应用和 API Key 通过关联表展示业务名称；此前页面错误读取原始用量记录接口，只包含 ID 字段，却按名称字段渲染，导致租户和 API Key 显示为空。
## 2026-07-27 企业服务模型发布预检

- 企业服务模型新增“发布检查”接口与控制台操作，逐项检查服务模型映射、路由生效状态、模型可见范围、候选部署生产准入与生效价格、供应商渠道和连接状态。
- 发布动作复用同一套预检；任一项异常时阻止发布，并合并返回具体异常项与处理提示，避免仅在实际调用时才暴露配置问题。
- 控制台检查结果弹框会展开预检响应中的 `checks` 明细，避免仅显示汇总对象导致检查项为空。
- 发布检查结果弹框宽度调整为 1180px，明细列按比例换行显示，优先避免水平滚动。
## 2026-07-27 模型部署生产状态操作合并

- 模型部署列表将“确认进入生产、拒绝生产、暂停生产”合并为“变更生产状态”单一入口；在弹框中选择目标状态并填写操作说明。
- 控制面新增统一生产状态流转接口，仍复用原有批准、拒绝、暂停的审计和生产准入规则。
- 操作说明调整为选填；有效成本价格结果弹框扩大至 1180px，表格按固定列宽自动换行，优先避免水平滚动。
## 2026-07-27 价格源管理列表调整

- 价格源管理的“更新时间”列宽调整为 120px；“立即同步”操作文案精简为“同步”，接口行为不变。
## 2026-07-27 价格源测试交互与解析报告修复

- 价格源“测试获取、测试解析”执行时显示进行中转圈并禁用同一按钮，避免重复提交。
- 解析报告统计卡采用边框盒模型，报告和表格限制在弹框宽度内；表格内容自动换行，修复右侧内容溢出。
## 2026-07-27 千问上下文缓存价格解析修复

- 修复千问阶梯价格表将“输入 Token 范围”误判为输入价格列的问题。
- 对官方页面明确标注“上下文缓存享有折扣”的模型，依据官方上下文缓存规则生成缓存命中（输入价 10%）和缓存写入（输入价 125%）价格组件；`qwen3.7-plus` 不再显示缓存写入价格尚未确认。
- 价格汇总字段在默认缓存组件为未知、但当前阶梯已存在明确缓存组件时，优先使用该阶梯的明确组件，避免目录页错误显示“尚未确认”。
## 2026-07-27 价格差异审核排序统一

- 价格差异审核后端查询和控制台默认排序统一为 `created_at`（前端字段 `createdAt`）降序，不再按风险等级优先排序。

## 2026-07-27 系统基础设置说明补齐

- 系统基础设置接口补充返回 `description` 字段，修复控制台“设置说明”列始终为空的问题。
- 为基准币种、预算币种、汇率自动更新、受管币种和汇率源补充用途、影响范围与修改注意事项；迁移会回填现有默认设置，缺失说明的新增设置也会展示安全的兜底说明。

## 2026-07-27 API Key 创建与列表空间优化

- Key 归属层级枚举统一精简为“应用级、项目级、租户级”，不在选项文本中附加括号说明。
- API Key 列表的操作列支持按页面配置宽度；本页从通用的 220px 收窄至 136px，将空间让给租户、项目、模型范围等业务列。

## 2026-07-27 内部成本单全租户查询修复

- 修复未选择租户或状态时，PostgreSQL 无法推断空参数类型而使成本单列表返回 500 的问题。
- 成本单生成、明细归集和异常检查的可选租户条件同步显式标注为文本类型，保证“全部租户”范围可查询和生成。

## 2026-07-27 内部成本单标题精简

- 移除内部成本单标题上方重复的“成本与预算”分类文案，保留标题与功能说明。

## 2026-07-27 系统基础设置列表列宽优化

- 系统基础设置的分组、设置键、当前值、敏感设置、更新时间和操作列改为固定宽度；“设置说明”列自动占用剩余空间，优先展示完整用途与影响说明。

## 2026-07-27 内部成本单明细名称展示

- 成本单明细和导出改为关联展示项目、应用、API Key 与供应商渠道名称，不再向用户展示上述资源的内部 ID。
- 关联资源已删除或历史记录未归属时，显示明确的未归属/未识别文字，不回退暴露内部 ID。

## 2026-07-27 内部成本单明细弹框适配

- 成本单明细弹框扩大至 1280px，并受视口宽度约束。
- 明细表按业务列固定比例分配宽度，名称自动换行，优先避免出现水平滚动条。

## 2026-07-27 预算规则审批入口与状态引导

- 高级治理菜单新增“治理审批”入口，用于处理预算、模型、价格和路由等高风险变更。
- 预算规则列表新增审批状态；提交后提示前往“高级治理 → 治理审批”，未获批准时不展示“审批后生效”操作，避免无入口提示和无效点击。

## 2026-07-27 预算审批状态与版本回滚修复

- 批准预算审批单时同步更新预算规则审批状态为“已批准”；V32 迁移会修复历史已批准但仍处于待审批的预算规则，使“审批后生效”按钮重新可用。
- 预算版本列表将字段名本地化，提交人关联显示账户显示名或账号，来源动作显示中文；版本弹框标题不再暴露预算规则内部 ID。
- 修复预算版本回滚中快照时间字符串未转换为 `timestamptz` 而导致的 500 错误。

## 2026-07-27 供应商渠道连接测试地址兼容

- 连接测试现在同时接受以 API 基础地址（如 `/v1`）或完整模型目录地址（如 `/v1/models`）保存的渠道配置；已存在 `/models` 后缀时不再重复追加，修复 Xiaomi MiMo 渠道请求 `/v1/models/models` 导致 HTTP 400 的问题。

## 2026-07-27 模型部署状态展示修复

- 模型部署的技术健康、成本价格、生产准入与最近探测状态补齐接口枚举的中文映射，不再展示 `UNAVAILABLE`、`CANDIDATE` 等内部编码。
- 技术健康为“健康”及成本价格为“已匹配官方价”时，状态标签按原始枚举值正确使用绿色成功样式。

## 2026-07-28 企业服务模型渠道映射一致性修复

- 企业服务模型的“已审核部署模型”下拉框现在按已选供应商渠道过滤，仅展示生产已批准的真实部署；切换渠道时会清空并重新加载模型选项。
- 保存企业服务模型时，控制面校验每个渠道与实际模型的对应部署，拒绝保存错配组合；路由策略候选加载也改为按生产准入状态筛选。
- V33 仅自动修复“单渠道、单模型且全库唯一可匹配”的历史错配记录，避免推断或覆盖存在多个候选渠道的业务配置。

## 2026-07-28 企业服务模型渠道名称展示

- 通用列表的多选关联值支持解析 JSON 数组并逐项映射选项文本；企业服务模型的供应商渠道现展示渠道名称，不再暴露内部渠道 ID。

## 2026-07-28 API Key 列表默认排序

- API Key 页面显式按 `createdAt` 降序请求列表，与控制面 `created_at DESC` 的默认排序保持一致，最新创建的 Key 优先显示。

## 2026-07-28 告警类型与对象类型中文化

- 告警页面为模型价格、价格源同步、供应商价格覆盖、模型发现和缓存价格等告警类型补充中文映射；模型部署、价格源、供应商渠道等对象类型同步中文展示。

## 2026-07-28 工作台待处理风险精简

- 工作台的待处理风险仅展示发生时间、中文告警类型和告警标题，按发生时间降序排列；列表区域固定高度并支持纵向滚动。
- 工作台加载告警数据时统一过滤信息级告警，待处理数量、运行摘要和风险列表均不再计入该类告警。
- 控制面告警查询同步排除 `INFO` 级别，进入当前告警或历史告警页面时与工作台保持一致。
- 工作台待处理风险将告警类型替换为对象类型，并增加告警级别；告警中心与工作台按级别展示严重/高（红）、警告（黄）、信息（蓝灰）状态颜色。
- 修正待处理风险列表继承旧版绝对定位样式造成的列错位；告警级别现参与网格布局，告警标题可使用剩余宽度完整展示。
- 待处理风险的固定列表高度由 236px 收紧为 176px，保留纵向滚动以减少工作台无效留白。

## 2026-07-28 租户工作台权限提示修正

- 租户工作台在租户成员关系缺失导致 403 时，明确提示“没有有效的租户成员关系”及处理路径，不再误导为缺少平台管理员权限。

## 2026-07-28 V34 迁移与混合重启

- 已通过 Control Plane 启动期 Flyway 执行 `V34__zhipu_official_price_adapter.sql`，迁移记录 `34 / zhipu official price adapter / success=true`。
- 混合部署的 Console、Control Plane、Gateway Runtime 与 Runtime Core 健康检查均返回 200。

## 2026-07-28 智谱价格差异发布修复

- V36 将 `provider_price_component` 的计费单位约束补充 `TOKEN_SECOND`，与缓存存储价格组件的既有应用层规范保持一致，修复智谱 GLM 价格差异批准发布时的约束错误。
- 通用文档价格适配器及其 LLM 抽取器为正式构造函数显式标注 Spring 注入，避免测试辅助构造函数导致控制面启动失败。
- 由于并行新增的未执行迁移与已执行 V36 冲突，已将其无内容改动地顺延为 V37，避免 Flyway 重复版本并保留全部业务建表语义。
- 混合重启后，Flyway 记录 V34～V37 均为 `success=true`；Console、Control Plane、Gateway Runtime 和 Runtime Core 健康检查均返回 200。已确认数据库约束允许 `TOKEN_SECOND`，`glm-5.2` 差异记录仍为待发布状态，可重新执行批准发布。

## 2026-07-28 GLM 路由别名校验修复

- V38 将历史服务模型别名 `GLM -5.2` 规范为 `GLM-5.2`，并同步修正路由和可变模型授权范围中的引用；调用事实记录不做改写。
- 企业服务模型保存时新增与路由和网关一致的别名格式校验，阻止包含空格的别名再次进入配置。
- 混合重启后，V38 的 Flyway 记录为 `success=true`；Console、Control Plane、Gateway Runtime 与 Runtime Core 健康检查均返回 200。

## 2026-07-29 用量分析图表悬浮指标

- 用量趋势图和应用请求趋势图新增按时间点的悬浮提示，分别展示请求数、Token、成本，以及各应用的请求数；离开图表即自动隐藏。

## 2026-07-29 价格源供应商类型必填

- 价格源管理表单将“供应商类型”纳入必填字段并展示必填星号；控制面同步拒绝未指定供应商类型的创建或更新请求，避免生成无归属的价格源。

## 2026-07-29 参考价格状态页面优化

- 顶部说明明确公开参考价格仅用于模型选型和成本估算，不作为实际结算依据，并移除重复提示框。
- 桌面端页面取消全局滚动，当前模型参考价格列表改为独立滚动区域；窄屏保留自然页面滚动以确保内容可访问。

## 2026-07-29 参考价格覆盖率统计修复

- 覆盖率分子改为按标准模型标识关联的有效公共模型数，不再拼接供应商类型与模型名，避免模型名自带供应商前缀时重复计数并导致覆盖率超过 100%。

## 2026-07-30 模型部署列表查询优化

- 模型部署列表的参考价匹配改为直接查询 `public_model_price_reference` 及价格源，不再对每条部署重复计算公共价格视图；保持原有模型、区域、当前价格和优先级规则，显著降低列表响应时间。
## 2026-07-30 路由候选生产准入过滤修复

- `GET /api/channel-model-deployments` 增加并实际使用 `productionStatus` 参数；路由策略页面请求的 `productionStatus=APPROVED` 现只返回已批准进入生产的渠道模型部署，避免候选下拉框出现未完成生产准入的发现模型。
## 2026-07-30 路由自动参考价格兼容

- 路由候选不再要求人工创建的 `price_version` 或正式成本价格；模型部署通过生产准入和真实探测即可进入路由。Console 将原“价格版本”下拉框改为只读“参考价格”，按所选渠道部署查询并展示自动参考价；无参考价或查询失败仅作提示，不阻止路由配置和生效。
## 2026-07-30 路由参考价格样式修复

- 路由候选的参考价格改为与下拉框一致的 32px 单行展示，超长内容以省略号呈现并可悬浮查看完整文本；价格摘要缩短为“参考价 / 币种 / 入 / 出”，避免换行撑高候选行。
## 2026-07-30 豆包路由模型映射修复

- 新增 Flyway V43，将既有企业服务模型 `doubao-seed-2.0-lite` 的实际模型从未完成生产准入的 `doubao-seed-2-0-lite-260428` 对齐为同一豆包渠道下已批准、健康且路由可用的 `doubao-seed-2-0-lite-260215`，修复路由候选不属于服务模型映射的校验异常；路由生效错误提示同步移除已废弃的“价格版本”前置说明。

## 2026-07-30 缺少参考价格不再暂停生产

- 供应商价格目录同步仅更新部署的价格状态；公开参考价缺失不再自动将已批准部署暂停生产或撤销路由资格，符合参考价格只用于选型和成本估算的产品规则。
- 新增 Flyway V46，仅恢复因旧价格同步逻辑误暂停、且仍为“已审核、健康、最近真实探测通过、管理员确认可进入生产路由”的部署；人为暂停及探测失败的部署不受影响。

## 2026-07-30 路由信息即时展示与时区统一

- 企业服务模型列表的“当前路由”改为直接展示控制面已同步的路由名称，不再依赖路由 ID 下拉缓存，路由保存并生效后的改名会在列表刷新后准确显示。
- 路由策略的创建时间和更新时间复用 Console 全局时间格式化，按浏览器本地时区转换带时区的接口时间，不再直接截断 UTC 时区信息。

## 2026-07-30 退役路由的故障切换展示

- 路由策略退役后，Fallback 列统一显示“停用”，并增加“路由已退役，故障切换不生效”的悬浮说明；仅调整展示，不修改保留的历史路由配置。
- 列名统一使用中文“故障切换”；启用显示绿色，手动停用显示灰色，因路由退役而不可用显示红色。

## 2026-07-30 路由策略操作列换行修复

- 路由策略操作列改为明确的可换行 Flex 按钮组，统一按钮间距和最小列宽，避免内联按钮换行时出现异常横向分隔效果。
- 按钮组改置于表格单元格内部，避免将 `td` 本身设为 Flex 而破坏表格布局和行分隔线。
## 2026-07-30 Gateway 自动参考价回退

- Gateway 解析路由成本时，优先使用有效的合同价、渠道实际价或供应商官方价；不存在人工 `price_version` 时，改为精确匹配当前有效的公共自动参考价，不再因旧价格版本前置条件拒绝调用。参考价作为 `PUBLIC_REFERENCE` 写入成本快照来源，不伪造价格版本 ID；参考价格组件同步规范化为网关计费结构。未匹配有效成本价或自动参考价时才返回 `TOKENSEA_PRICE_NOT_CONFIGURED`。

## 2026-07-30 首次部署默认管理员初始化

- 控制面启动时仅在 `user_account` 为空且引导状态未完成时自动创建文档中的 `admin` 默认账号并授予 `ADMIN` 角色；已有用户、已有引导状态及密码不会被覆盖。
- 新增 `TOKENSEA_DEFAULT_ADMIN_*` 配置项，可在 Compose 部署前覆盖默认账号或禁用该行为；新增单元测试覆盖首次创建和已有部署不变两种情况。

## 2026-07-31 Compose 环境模板补齐

- `.env.example` 补充 Compose 内部 `TOKENSEA_RUNTIME_CORE_BASE` 默认地址，确保复制模板后可显式查看并按需覆盖 Gateway 到 Runtime Core 的内部连接配置。
- 模板继续保留生产安全默认值：本地测试上游默认关闭，出口仅允许标准 HTTP/HTTPS 端口；不将本机的 `host.docker.internal` 与开发测试端口写入后续部署模板。
