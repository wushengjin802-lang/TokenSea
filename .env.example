# 已弃用：此文件仅为兼容旧链接而保留，不得直接传给 Docker Compose。
#
# 唯一权威环境变量模板：deploy/compose/.env.example
# 请从 tokensea/deploy/compose 目录执行：
#   Copy-Item .env.example .env
# 然后编辑 .env，并使用：
#   docker compose -p tokensea --env-file ./.env up -d --build
#
# 不要复制本文件生成部署配置，也不得把根目录兼容文件传给 Compose。
